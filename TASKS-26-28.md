# משימות 26–28 — הפער הרב-גאנגי, מקצה לקצה

מסמך אחד, ארבע משימות, סדר מחייב. כללי הברזל של `CLAUDE-CODE-TASKS.md`
חלים כלשונם: משימה אחת בכל פעם, קומיט אחד למשימה, אל תיגע בקבצים
מחוץ להיקף, אמת את הממצא לפני שאתה מתקן, אל תתקדם על אימות שנכשל,
אל תשנה טסטים כדי שיעברו, בלי refactor.

**הפניות הן לקבצים ולסמלים, לא למספרי שורות.** מספרי השורות במסמך
הזה נבדקו על `6168f46` וזזו מאז.

---

## הסדר, ולמה הוא מחייב

```
26א  ← המפתוח פר-endpoint          חוסמת את כל השאר
26ב  ← bind לכל endpoint            אחרי 26א בלבד
27   ← מסך הוסף-מכשיר + זיהוי       אחרי 26א
28   ← זיהוי בנורית                 ❌ בוטלה 2026-08-28 — לא רצויה
```

**26ב לפני 26א** = גלגול רגרסיה קיימת לכל בית. היום היא בלתי-נראית
רק משום שה-`bind` נשלח ל-endpoint אחד, ולכן גאנג 2 לא מדווח ולא
משחית כלום. ברגע ש-26ב תרוץ, כל דיווח מגאנג 2 ידרוס את מצבו של
גאנג 1 — בתצוגה, ב-toggle, ובכלל ההתערבות הידנית.

**27 לפני 26א** = מסך שנותן למשתמש בדיוק את מה שביקש, שבור: שתי
הישויות יקראו מאותו תא, יציגו מצב זהה ושגוי, וה-toggle יישלח הפוך.
וכפתור הזיהוי עצמו ישקר (ראה 26א, השלכה 4).

---

## 26א — `_states` / `_expected` / `read_state` פר-(ieee, endpoint)

**בהיקף:** `src/smart_kosher/adapters/zigbee_gateway.py` בלבד.

### הממצא

`_on_state` כותב `self._states[ieee] = bool(payload["on_off"])` —
מפתח `ieee` בלבד. תא אחד למכשיר פיזי. אותו דבר ב-`_expected`.

**הודגם על חומרה**, בריצה שהוכיחה את משימה 12:

```
GANG2 on / waiting ep1 -> {'confirmed': False, 'observed': True} | reports: [(2, True)]
```

`observed: True` בזמן שגאנג 1 כבוי. הדיווח מגאנג 2 דרס את המצב
שמייצג את גאנג 1. הממתין סירב נכון — התא משקר.

### ארבע השלכות, לא אחת

| # | איפה | מה נשבר |
|---|---|---|
| 1 | `devices()` | התצוגה מראה את מצבו של הגאנג שדיווח אחרון |
| 2 | `_toggle` | קורא מאותו תא ובוחר כיוון — **שולח את הפקודה ההפוכה** |
| 3 | כלל ההתערבות הידנית | `_expected` נושא בדיוק אותו פגם מפתוח |
| 4 | `read_state(ieee)` | מקבל `ieee` בלבד וקורא `entry.get("endpoint", 1)` — **אין דרך לקרוא את גאנג 2 בכלל** |

השלכה 4 היא זו שהופכת את זה מ"תיקון תצוגה" למשהו שמדליק אור
בבית של מישהו ולא יודע להחזיר אותו. כפתור הזיהוי במשימה 27
נשען עליה.

### מה לעשות

`_states`, `_expected`, `_state_ms` ו-`read_state` יתמפתחו לפי
`(ieee, endpoint)`.

**אל תמציא את מקור ה-endpoint — הוא כבר קיים:**

* `_state_ep` — נוסף במשימה 12, נושא את ה-endpoint של הדיווח.
* `confirm_target(entity_id)` — נוסף במשימה 25, מחזיר `(ieee, endpoint)`.
* `_endpoint_admits` — כלל ההשוואה. **תאימות אחורה לדיווח בלי
  endpoint עוברת דרכו, בדיוק כמו במשימה 12. אל תכתוב כלל שני.**

`read_state` מקבל פרמטר endpoint. ברירת מחדל שומרת על הקוראים
הקיימים.

### הוכחה — שלוש מוטציות, כל אחת חייבת להאדים

* **M1:** החזר את `_states` למפתוח `ieee` בלבד. דיווח מגאנג 2 חייב
  להשחית את התצוגה של גאנג 1.
* **M2:** החזר את `_toggle` לקריאה מהתא המשותף. toggle על גאנג 1
  אחרי דיווח של גאנג 2 חייב לשלוח את הכיוון ההפוך.
* **M3:** החזר את `read_state` להתעלמות מה-endpoint. קריאה של גאנג 2
  חייבת להחזיר את מצבו של גאנג 1.

### אימות על חומרה

ה-DUT `70:d0:7e:…` הוא הריג היחיד שיכול להוכיח את זה — ה-`bind`
על endpoint 2 נכתב אליו וחי בטבלת ה-Zigbee שלו. **אל תבטל אותו.**
קרא את ה-`short_addr` מהרישום, אל תקבע אותו — הוא התחלף ל-`0x3c20`
בצימוד מחדש.

---

## 26ב — `_request_reporting` לכל endpoint

**בהיקף:** `zigbee_gateway.py` בלבד. **רק אחרי ש-26א ירוקה.**

### הממצא

`_request_reporting` שולח `enable_reporting` עם
`entry.get("endpoint", 1)` — endpoint אחד למכשיר. מכשיר רב-גאנגי
מקבל `bind` + `configure_reporting` לגאנג 1 בלבד, לנצח. לחיצה
ידנית על גאנג 2 לא מדווחת, וכלל ההתערבות הידנית לא חל עליו.

**זה לא "הפער הידוע במולטי-גאנג" שבנספח ב׳.** שם מתואר שה-`endpoint`
העליון הוא מה שמסלול הפקודה משתמש בו. שה-`bind` בכלל לא נשלח —
לא מתואר שם.

### מה לעשות

בקש reporting לכל endpoint ברשומה שיש עליו cluster 6.

שים לב ש-`_reporting_inflight`, `_reporting_retry` והקרדיטים
ממופתחים היום לפי `ieee`. אם שני endpoints של אותו מכשיר יבקשו
במקביל, הם ידרסו זה את זה. הכרע איך — ותעד את ההכרעה.

### הוכחה

* **M1:** החזר ל-endpoint אחד. מכשיר עם cluster 6 על שני endpoints
  חייב לקבל שתי בקשות, לא אחת.
* **חומרה:** על ה-DUT, אחרי איפוס ה-bind — שני הגאנגים מדווחים.

---

## 27 — מסך "הוסף מכשיר": רשימה עם זיהוי בפעימה

**בהיקף:** `products/panel/device/` — `main.py`, `room_page.py`,
ומסך חדש. **תלוי ב-26א** (כפתור הזיהוי קורא `read_state` עם endpoint).

**מחוץ להיקף:** זיהוי בנורית Zigbee. זו הייתה משימה 28, ו**היא בוטלה** ב-2026-08-28 — הזיהוי בהיפוך הממסר נשאר הדרך היחידה.

### מה קורה היום

`room_page._add_device` פותח `permit_join(180)` ומסמן
`store.pairing`. `main._try_autopair` חוטף את המכשיר הראשון שאין לו
ישות, יוצר ישות אחת בשם `"מכשיר N"`, ועושה `break`.

המשתמש לא בוחר, לא מזהה, ומכשיר דו-גאנגי מקבל ישות אחת.

### א. `_try_autopair` נמחק

לא "מתווסף עליו" — **נמחק**. כל עוד הוא רץ הוא חוטף את המכשיר
והרשימה תמיד תהיה ריקה. זה השינוי האמיתי, לא הוספת מסך.

### ב. `_devices_refresh` מרזה יותר מדי

הוא שומר ל-store רק `{on_off, unreachable, endpoint}` ומשליך
`clusters` ו-`endpoints`. המסך החדש לא יכול לגזור את רשימת הגאנגים
בלעדיהם.

הוסף מה שצריך — ושמור על הכוונה המקורית של השורה הזאת, שכתובה
בהערה מעליה: לא לכלול שדות שמשתנים בכל poll (`state_age_ms`),
אחרת כל poll יצייר מחדש.

### ג. מבחן שלמות הגילוי — אל תדלג על זה

ההערה ב-`_on_device_endpoints` אומרת:

> "device_joined always reports **endpoint 1** because that is all it
> knows at announce time"

רשימת ה-endpoints והקלאסטרים מגיעים **אחרי** ההצטרפות, בהודעות
נפרדות. מסך שיצייר ברגע ה-join יראה מכשיר דו-גאנגי כחד-גאנגי,
המשתמש יוסיף רק את גאנג 1 — **וזה בדיוק הבאג של היום בלבוש חדש.**

מבחן השלמות: `endpoints` קיים ברשומה, **ולכל endpoint ברשימה יש
מפתח ב-`clusters`.** עד אז השורה מציגה "מזהה יכולות…" ולא מספר
גאנגים.

### ד. הרשימה

שורה אחת לכל **גאנג**, לא לכל מכשיר:

> endpoint ברשומה שיש עליו cluster 6 (OnOff), שאין לו כבר ישות.

אומת מול המכשירים האמיתיים:

```
70:d0:7e:…  endpoints [1, 2]     cluster 6 על שניהם  → שתי שורות
78:1c:9d:…  endpoints [1, 242]   cluster 6 על 1      → שורה אחת
                                 (242 = Green Power)
```

תלת-גאנג יגיע כ-`[1,2,3]` ויקבל שלוש שורות בלי שורת קוד חדשה.

**"אין לו כבר ישות"** = אין ב-`endpoints.list` רשומה עם אותו
`ieee_address` **ואותו** `zigbee_endpoint`. לא לפי `ieee` בלבד —
זו בדיוק ההנחה שגרמה לגאנג 2 לא להופיע לעולם.

### ה. כפתור "זהה" — בפעימה

```
קרא את מצב ה-endpoint הזה  →  הפוך  →  המתן  →  החזר
```

הקריאה חייבת להיות פר-endpoint (26א). **בלעדיה** הקריאה מחזירה את
מצבו של גאנג 1, ההיפוך נשלח לגאנג 2, וההחזרה מגדירה את גאנג 2
להיפוך של גאנג 1 — המנורה נשארת במצב אקראי.

**חובה להחזיר.** אתה מדליק עומס אמיתי בבית של מישהו. אם ההחזרה
נכשלה — הודע למשתמש, אל תשתוק.

### ו. הוספה

יוצרת ישות `(ieee, endpoint)` עם ה-`zone_id` מ-`store.pairing`.

השם: **אל תשתמש ב-`"מכשיר {}".format(len(endpoints) + 1)`** — הוא
מייצר "מכשיר 2" למכשיר שאינו השני. תן למשתמש לתת שם, או גזור
מהחדר והגאנג.

### ז. זריקה מהרשימה

מחק את הרשומה מהרישום **מקומית בלבד**. אל תשלח `remove_device` —
זו משימה 21, והיא מאשרת `ok` ללא תנאי. אל תשעין זרימת משתמש על
הפעולה היחידה בקושחה שאנחנו יודעים שאין לה סולם ACK.

### הוכחה — שלוש מוטציות

* **M1:** הסר את סינון cluster 6 → endpoint 242 מופיע כגאנג.
* **M2:** החלף "אין ישות ל-(ieee, endpoint)" ב-"אין ישות ל-ieee"
  → גאנג 2 נעלם מהרשימה. **זו המוטציה המרכזית.**
* **M3:** החזר את `_try_autopair` → הרשימה ריקה כשהחלון פתוח.

ובנוסף: הסר את מבחן שלמות הגילוי (ג) → מכשיר שהרגע הצטרף מוצג
כחד-גאנגי.

### חומרה

מול ה-DUT הדו-גאנגי: שתי השורות מופיעות, והזיהוי מזיז את המנורה
הנכונה ומחזיר אותה.

---

## 32 — `forget_device` יקשיב לפסק הדין, ויסדר את הסדר

**חומרה: P2. אחרי 21, לא ממוזגת לתוכה.** 21 היא קושחה, 32 היא אדפטר — שני
קבצים, שני קומיטים.

**בהיקף:** `src/smart_kosher/adapters/zigbee_gateway.py` (`forget_device`).

### הממצא

```python
def forget_device(self, ieee):
    self._registry.pop(ieee, None)          ← שוכח קודם
    self._save_registry()
    self._write_cmd("remove_device", …)     ← fire-and-forget
    return {"removed": True}                ← ללא תנאי
```

**שני פגמים, ושניהם שורדים את 21:**

1. **אף אחד לא מקשיב.** `_write_cmd` אינו יוצר רשומה ב-`_pending`, ולכן ה-ack
   חוזר עם `request_id` שאיש לא ממתין לו **ונזרק בשקט**. גם אחרי ש-21 תלמד
   את הקושחה להחזיר `delivered`/`failed` אמיתי, הפסק דין לא מגיע לאיש. אותה
   צורה בדיוק שנמצאה ב-`_request_reporting`.
2. **הסדר הפוך.** הרשומה נמחקת **לפני** השליחה, אז `leave` שנכשל משאיר מכשיר
   ששכחנו — והוא עדיין על הרשת.

### מה לעשות

`await self._command("remove_device", …)`, ורק בהצלחה למחוק. **שלח, המתן, ואז
שכח.** שים לב ש-`forget_device` סינכרונית היום ויש לה קוראים
(`_release_radio_device` ב-`api.py`, ו-`discard_device` **אינה** נוגעת בזה —
היא מקומית בכוונה).

### הוכחה

* **M1:** החזר את הסדר → `leave` שנכשל משאיר רישום מחוק ומכשיר על הרשת.
* **M2:** החזר ל-`_write_cmd` → הפסק דין נזרק ו-`removed: True` חוזר תמיד.

---

## 36 — "מחפש מכשיר" לא נגמר לעולם  ⛔ רגרסיה מ-27

**חומרה: P1. ראשונה.** **בהיקף:** `room_page.py`, `main.py`.

`store.pairing` מנוקה **רק** ב-`room_page.py:71`, בלחיצה חוזרת על הכפתור.
לפני 27, `main._try_autopair` עשה `store.pairing.set(None)` אחרי אימוץ. 27
מחקה אותו **בצדק** — זו הייתה המשימה — ואיש לא החליף את הניקוי.

וגרוע יותר: `permit_join(180)` פג ברכזת אחרי שלוש דקות והמסך ממשיך לומר
"מחפש מכשיר (ביטול)". **התווית משקרת על מצב הרשת.**

**מה לעשות.** החלון הוא 180 שניות — שהמצב יפוג איתו, והתווית תספר את האמת
("מחפש… 2:14") במקום בוליאני שנתקע.

**לפני שממציאים טיימר:** יש אירוע `permit_join_status` מהקושחה. בדוק אם הוא
כבר נותן את הסוף. **אמת מהחוט עדיפה על שעון מקומי.**

*מוטציה:* הסר את הפקיעה → המצב נתקע שוב.

---

## 37 — אין כפתור "חבר"  ⛔ פער ב-27

**חומרה: P1.** **בהיקף:** `add_device_page.py`.

`:195` — ההוספה תלויה על הכרטיס עצמו:

```python
card.add_event_cb(lambda e, r=row: _add(r), lv.EVENT.CLICKED, None)
```

ולידו **שני כפתורים גלויים**, "זהה" ו"הסר מהרשימה" (`:199-204`). הפעולה
הראשית היא היחידה בלי כפתור, **והמשתמש לא מצא אותה.**

**מה לעשות.** כפתור "חבר" מפורש בשורה, בולט משני האחרים.

---

## 38 — אייקונים מוצגים כמלבן  · **פרוב לפני תיקון**

**בהיקף:** `theme.py`, ואז מה שהפרוב יקבע.

הם **אינם** `LV_SYMBOL` — הם תווים טיפוגרפיים בפונטי Assistant:

| תו | קודפוינט | מופעים |
|---|---|---|
| `›` | U+203A | 7 — חץ החזרה בכל תת-מסך (`shell.py:59`) |
| `‹` | U+2039 | 2 (`widgets.py:63, 100`) |
| `—` | U+2014 | **48** — מצב "לא ידוע" |
| `·` | U+00B7 | 11 |
| `…` | U+2026 | 4 |
| `±` | U+00B1 | 1 |

ובנוסף שני `LV_SYMBOL` אמיתיים מ-`FONTS.symbol`: BACKSPACE ×4, HOME ×1.

`theme.py:41` קורא לקובצי ה-`.bin` "Hebrew-ranged", וכל התווים למעלה מחוץ
לטווח עברית+ASCII. **זו השערה שלא נמדדה.** והלוגו אינו ראיה — `ui_home.py:53`
אומר "solid primary + house glyph", כלומר ריבוע צבוע; הגליף שעליו כנראה מלבן
גם הוא, על רקע מלא.

### שלב א׳ — פרוב, לפני כל תיקון

**אל תנסה לפרסר את קובצי ה-`.bin`.** ניסיון כזה החזיר 6,840 גליפים בקובץ של
5KB — כלומר לא מדד כלום. שאל את הפונט עצמו, על הלוח:

```python
dsc = lv.font_glyph_dsc_t()
theme.FONTS.body.get_glyph_dsc(dsc, cp, 0)
```

על `0x203A 0x2039 0x2014 0x00B7 0x2026 0x00B1 0xF55A 0xF015`, מול `body`
ומול `symbol`. **דווח את הרשימה לפני תיקון.**

### שלב ב׳ — ההכרעה, ומה שמייקר אותה

**לקובצי הפונט אין מתכון בנייה בריפו.** חמישה `.bin` ב-
`products/panel/device/fonts/`, בלי `lv_font_conv`, בלי מפרט טווח, בלי TTF
מקור. **אותה מחלקה כמו 0.11.1: נכס שנשלח ואי אפשר לייצר מחדש.**

1. **להחליף תווים בכאלה שבטווח** — "חזרה" בעברית במקום `›`, מקף ASCII במקום
   `—`. זול, בלי תלות בנכס.
2. **לבנות מחדש עם טווח מורחב** — ואז המתכון נכנס לריפו, אחרת יצרנו את אותה
   בעיה לדור הבא.

**הנטייה: 1 לתיקון, 2 כמשימה נפרדת** — אין סיבה שתיקון תצוגה יחכה לשחזור
נכס. הפרוב מכריע.

### שלב ג׳ — גילוי, לא אופציונלי

`load_fonts` בודק ש-`_load_binfont` לא החזיר `None`, ולא שהפונט **נושא את
התווים שהממשק מצייר**. וב-`theme.py:88` אין אפילו את זה:

```python
FONTS.symbol = getattr(lv, "font_montserrat_28", None) or lv.font_montserrat_16
```

השאלה היא "האם אובייקט הפונט קיים", לא "האם יש בו את הגליף". **הוסף בדיקה על
קומץ קודפוינטים והדפס אם חסר** — בלי זה הבנייה הבאה תשבור את זה שוב בשקט.

**השלכה על 33:** אם U+2014 חסר, ה-`—` שמשמעותו "לא ידוע" הוא מלבן ב-48
מקומות. כשמתקנים — לוודא שהמצב נראה כמו "לא ידוע" ולא כמו תקלה.

---

## 39 — אין מספרים במקלדת

**בהיקף:** `text_input.py`. **שורה אחת.**

`keyboard.py:23` כבר מכיל `("1","2","3")` — הפריסה הנומרית קיימת.
`text_input.py:102` מעביר `LAYOUT_HEBREW` בלבד.

**"מכשיר 2" ו"סלון 1" אינם ניתנים להקלדה היום** — וזה בדיוק השם שהצימוד
מייצר.

---

## 40 — אין סמן בשדה הטקסט

**בהיקף:** `text_input.py`.

`:90` — `_field` הוא `w_label`, לא `lv.textarea`. **לתווית אין סמן, מבנית.**

**בדוק לפני שעוברים ל-`lv.textarea`:** הוא מביא גלילה משלו, ו-CLAUDE.md אוסר
גלילה על הפאנל. אם מתנגש — קו נטוי מהבהב בסוף הטקסט מספיק. בלי אנימציה
מלאת-מסך.

---

## 41 — גובה שורה קשיח

**בהיקף:** `room_page.py`.

`set_height(64)`, `set_size(120, 64)`. המבוקש: רבע מהגובה הזמין, מותאם.

**זהירות:** ל-`hwtest_ui` יש "אף רשימה לא גולשת מהזכוכית" ו"עמוד מחזיק מספיק
שורות". גובה יחסי משנה כמה נכנס. **הרץ ותראה מה נשבר — ואם משהו נשבר, זו
הכרעה ולא תקלה: תגיד מה בחרת ולמה.**

---

## 42 — פסים מהמסך הקודם נשארים במעבר  · **פרוב לפני תיקון**

**המבני והחמור מכולם.**

כל המסכים נטענים דרך `lv.screen_load` על אובייקט **מטמון** שנבנה פעם ומנוקה
ב-`clean()` (`add_device_page:218`, `room_page:280`, `device_page:184`,
`pages:62`). החשד: LVGL מצייר מחדש רק אזורים שסומנו כלא-תקפים, ומסך ממוחזר
לא מסמן הכול — ועם RGB DMA זה משאיר שורות ישנות בפריים.

**הפרוב הראשון, ולא תיקון:** אחרי `screen_load`, קרא
`lv.screen_active().invalidate()`.

* **נעלמים** → פער סימון, תיקון קטן ומקומי.
* **נשארים** → שכבת DMA/פריימבאפר — למדוד לפני שנוגעים.

**אל תוסיף אנימציית מעבר כדי לכסות** — CLAUDE.md אוסר אנימציה מלאת-מסך, וזה
מסתיר סימפטום. **רק העין תופסת את זה, אז כל תיקון דורש אישור מהמסך.**

### נמדד 2026-08-25 — הפרוב שלמעלה כנראה מיותר, והתשובה כבר בזמנים

לא הרצתי את `invalidate()`, כי מדידה זולה יותר מכריעה לפניו.

| מה | ערך |
|---|---|
| מחזור פריים של הפאנל | **22.8 ms** (PCLK 18MHz; H 800+8+8+4=820, V 480+8+8+4=500 ⇒ 410,000 px ⇒ 43.9Hz) |
| ציור מחדש של מסך מלא | **114.8 ms** חציון (6 דגימות: 110.2 · 114.2 · 114.8 · 114.8 · 114.8 · 118.9) |
| היחס | **~5 פריימים לכל מעבר** |

הטווח שנכתב לפני ההרצה היה 30–200 ms; התוצאה בתוכו.

**המסקנה:** מעבר מסך *אינו יכול* להיות אטומי. הפאנל סורק את ה-framebuffer
כחמש פעמים בזמן ש-LVGL עדיין כותב לתוכו, ולכן חלק מהמסך מציג תוכן ישן וחלק
חדש — וזה בדיוק "פסים".

ומכאן ש-`invalidate()` לא רק שלא יעזור, הוא **יכול להזיק**: הוא מגדיל את
השטח שיש לצייר, כלומר מאריך את החלון שבו שתי התמונות חיות יחד. בעץ ההחלטות
של המשימה זה הענף השני — "שכבת DMA/פריימבאפר, למדוד לפני שנוגעים" — וזו
המדידה.

**מה שהמדידה עדיין לא אומרת:** האם הדרייבר מרכיב לתוך ה-framebuffer שנסרק
*ברגע זה*, או מחליף בין שניים. `display.py` מתאר "שני framebuffers מלאים
מוסתרים ב-C" עם העתקה מ-core 2. אם יש החלפה אמיתית ב-vsync לא היו פסים —
העין אומרת שיש. לפני נגיעה ב-framebuffer צריך לאמת את זה ישירות; CLAUDE.md
מזהיר על בדיוק השכבה הזאת (LoadProhibited boot-loop).

### ⛔ 42 אינה סגורה — נסגרה על קריאה שגויה שלי

**המדידה נשארת עובדה. המסקנה שנבנתה עליה יורדת.**

הדיווח המקורי אמר "נשאר מספר פסים לרוחב ממסך קודם". קראתי "נשאר" כ"נראה
רגע במעבר", ומדדתי האם המעבר אטומי. אברהם חזר ואישר: **הפסים נשארים.**
לא הבהוב — שארית שיושבת על הזכוכית אחרי שהמעבר נגמר.

114.8 ms מול 22.8 ms מוכיח שמעבר **אינו אטומי**, וזה נכון. זה גם לא מסביר
שום דבר על שארית: קרע נעלם בפריים הבא. שארית לא. **מדידה נכונה שענתה על
שאלה שלא נשאלה** — וזו וריאציה על "ב. המספר אמיתי, השאלה אחרת" ב-CLAUDE.md,
אלא שהפעם השאלה השגויה הייתה שלי ולא של הכלי.

הכשל היה בשכבת הפרשנות, לא במדידה. ההמשך: **משימה 47.**

### ✅ 42 נסגרה 2026-08-27 — אותו שורש כמו 48

`rotate0` לא כתב את השורה האחרונה של כל רצועת flush (`y < y_end` על גבול
**כולל**), ולכן כל אזור שלא צויר מחדש ברוחב מלא נשאר עם שורות שאיש לא כתב.
זו גם ה"שארית" הזאת. תוקן ואומת על הזכוכית — אברהם: *"אין פסים ואין שאריות."*

הפירוט: **משימה 51**. הפאץ': `products/panel/firmware/lvgl_micropython/`.

**שתי הקריאות השגויות שלי כאן מוצדקות בדיעבד בכיוון אחד ולא בשני:** "מעבר
אינו אטומי" נכון ולא רלוונטי; "מיקום שזז ⇒ תזמון" פשוט שגוי — הרצועות הן
גיאומטריה, ומיקומן זז כי הוא תלוי במה צויר מחדש לפני כן.

---

## מטלה קטנה — `deploy.ps1` ימצא את הלוח לבד

`-Port` חובה, בזמן ששלושת רצי ה-hwtest קוראים `find_panel()`. הדוקסטרינג של
`run_hwtest.py` מציין את הקפיצה COM7↔COM8 כאחת משלוש המטלות שהוא פותר.
`deploy.ps1` הוא היחיד בתיקייה שלא קיבל את זה, **והוא הפיל פריסה ב-2026-08-24.**

`find_panel()` כברירת מחדל, `-Port` נשאר כעקיפה. שורה אחת.

---

## 28 — ❌ בוטלה 2026-08-28. לא רצויה

**החלטת מוצר של אברהם, לא ממצא.** הזיהוי שנבנה במשימה 30 — היפוך הממסר,
החזקה, והחזרה ב-`finally` — עושה את העבודה ואומת על החומרה. הבהוב נורית היה
נעים יותר, ולא הצדיק op חדש בקושחה, בנייה וצריבה.

הרקע נשאר רשום כי הוא עובדה על החומרה ולא תוכנית: **המכשיר תומך** — cluster 3
קיים על שני ה-endpoints של ה-DUT — ורק הקושחה לא יודעת לשלוח. אם זה יעלה שוב,
זה מתחיל מ-`cmd_identify` ב-`zb.c` לפי התבנית של `cmd_on_off`.

התיאור המקורי, לתיעוד:

### 28 — זיהוי בנורית (שיפור נוחות, **לא חוסם**)

> **עודכן:** `zigbee.identify` נבנה במשימה 30 ומזהה **בהפעלת הממסר עצמו** —
> קריאה, היפוך, החזקה, החזרה ב-`finally`, עם נעילה פר-ieee. אומת על ה-DUT
> בשני הגאנגים עם מנורה אמיתית. **זרימת ההוספה שלמה.** 28 מחליפה את ההיפוך
> בהבהוב נורית, שהוא נעים יותר ולא מפעיל עומס — אבל שום דבר לא ממתין לה.

אין `identify` ב-ops של הקושחה. שמונה בלבד: `ping`, `permit_join`,
`on_off`, `read_attr`, `read_report_cfg`, `enable_reporting`,
`remove_device`, `bind`.

המכשיר **כן** תומך — cluster 3 קיים על שני ה-endpoints של ה-DUT.
רק אנחנו לא יודעים לשלוח.

דורש op חדש ב-`zb.c` ובנייה ב-ESP-IDF. **לצד משימה 21.**

---

## חסמים עומדים, לא משימות

| מה | פותח |
|---|---|
| ~~בנייה אחת ב-ESP-IDF~~ | ✅ **בוצעה 2026-08-23.** שתי בניות מלאות ב-WSL קימפלו את `zb.c` עם התיקון של 21, ולכן **19, 20 ו-21 הן compile-verified**. השורה הזאת נשארה כאן אחרי שהחסם נסגר — בדיוק כמו שורת `.gitattributes` מתחתיה, ומאותה סיבה |
| ~~`.gitattributes`~~ | ✅ **תוקן 2026-08-20 ואומת 2026-08-27:** `bash firmware/h2_coordinator/host_test/run.sh` מחזיר `1247 checks, 0 failures`. השורה הזאת נשארה כאן אחרי שהחסם נסגר, והטעתה את משימה 55 |

---

## 44 — חליפת ה-hwtest מחזירה 0 גם כשהיא אדומה

**נמצא תוך כדי 38.** `mpremote 1.28.0` **אינו** מעביר את קוד היציאה של המכשיר
למארח. נמדד ישירות:

```
mpremote exec "raise SystemExit(1)"  ->  EXIT=0
mpremote exec "raise SystemExit(0)"  ->  EXIT=0
```

ולכן `run_hwtest_ui.py` — שכל הציון שלו הוא
`raise SystemExit(0 if hwtest_ui.run() else 1)` — יוצא **0 גם כשהחליפה נכשלה**.
אומת: הרצת המוטציה של 38 הדפיסה `79 of 80 passed` + `FAILED:` והחזירה `EXIT=0`.
אותו דבר ב-`run_hwtest.py`.

`run_hwtest_gangs.py` **לא** נפגע: הוא רץ עם `capture=True` וגוזר את שלושת
קודי היציאה שלו מהפלט במארח. שלושת הקודים שהוכחו שם תקפים.

**המשמעות:** כל שער שנשען על קוד היציאה של חליפת הלוח ירוק תמיד. זו בדיוק
המשפחה של "ירוק כוזב במנגנון האימות" — אבל בשכבה שמעל הבדיקות, ולכן היא
מכסה את *כולן* בבת אחת.

**תוקן 2026-08-25.** כל חליפה מדפיסה שורה אחת בסוף — `HWTEST_RESULT pass`
או `HWTEST_RESULT fail <n>` — והמארח גוזר ממנה את התוצאה
(`run_common.suite_verdict`). הפלט זורם ונאסף במקביל, כדי שהשורה האחרונה לפני
תקיעה עדיין תיראה בזמן אמת.

**התכונה שנושאת את הכל: היעדר שורה = כישלון.** לוח שקרס, נתקע או אותחל באמצע
לא מדפיס כלום, ו"לא נאמר דבר" לעולם לא ייקרא כ"לא קרה דבר". שתי שורות = גם
כישלון, כי אז הפלט אינו מה שהפרסר חושב שהוא.

`hwtest.py` לא היה לו ציון **בכלל** — `run()` מחזיר `None`, ולכן גם קוד יציאה
תקין לא היה נושא ממנו כלום. עכשיו יש לו.

הלוגיקה טהורה ולכן חיה ב-CPython: `tests/test_hwtest_verdict.py` (10 טסטים),
כולל טסט הצמדה שקושר את המילה שהחליפות מדפיסות לזו שהמארח מחפש — import חסום
(MicroPython מול קוד מארח), ולכן טסט הצמדה, לפי הכלל ב-CLAUDE.md.

**מוטציות, אחת בכל פעם:**
* בדיקה אמיתית נכשלת → `HWTEST_RESULT fail 1`, הרץ יוצא **1**.
* השורה מוסרת מהחליפה → החליפה מדפיסה `88 of 88 passed` ובכל זאת הרץ יוצא
  **1** ואומר "the suite never reported a result". זו הבדיקה החשובה מהשתיים.

---

## 45 — deploy גוזם לפי רשימה ביד, והרשימה מכילה שם אחד

**תוקן 2026-08-25 — הניסוח הראשון שלי היה שגוי.** כתבתי "deploy מעתיק ואינו
גוזם". הוא כן גוזם, בשלב `3c`:

```powershell
foreach ($stale in @("scheduler.py")) {
    try { Mpr rm (":" + $stale) } catch { }
}
```

**רשימה ביד עם שם אחד.** `scheduler.py` נכנס אליה כשהוא זז ב-2026-08-05;
`house_page.py`, שנמחק ב-2026-08-02, מעולם לא נכנס — ולכן הוא עדיין על
הפלאש, **באותם 5308 בתים** כמו הגרסה שלפני המחיקה, ועדיין ניתן ל-import.

כלומר זו לא "אין גזימה" אלא **גזימה שמזדקנת** — בדיוק אותה משפחה של רשימת
הקודפוינטים שנכתבה ביד במשימה 38, ובדיוק אותו תיקון: **לגזור את הקבוצה
מההפרש** בין שורש הלוח לבין `device/`, במקום לזכור שמות.

**תוקן 2026-08-25.** שלב `3c` גוזר את היתומים במקום לזכור אותם. מה שמוגן:

| מה | למה |
|---|---|
| `device/*.py` | המוצר עצמו |
| `hwtest/*.py` | החליפות שהרצים מעלים — גזירה מול `device/` בלבד מוחקת אותן |
| `boot.py` | הסטאב של MicroPython עצמו, קיים בפלאש טרי ואינו שלנו |

רק `.py` נשקל, וזה מה שמשאיר את הפונטים (`*.bin`) ואת `lib/` ו-`data/` בחוץ
בלי לנקוב בשמם.

**נמדד:** `orphan -> house_page.py`, ורק הוא. אחרי הפריסה יש בשורש **36**
קבצי `.py` = 32 (device) + 3 (hwtest) + 1 (boot), והחליפה רצה 88/88.

**מוטציה:** הסרת שורת ה-`hwtest` מקבוצת המוגנים גורמת ל-deploy למחוק את
**שלוש** החליפות (`orphan -> hwtest.py`, `hwtest_ui.py`, `hwtest_zmanim.py`).
כלומר החצי השני של ההגדרה נושא משקל — הוא לא נימוס.

**המשמעות שלא השתנתה:** מודול מת שנשאר על הלוח עדיין ניתן ל-import. קוד ישן
שנשאר נגיש הוא הדרך שבה "תיקנו את זה" הופך ל"תיקנו את זה בקובץ אחר".

---

## 46 — שחזור `main.py` דיווח הצלחה והשאיר קובץ של 0 בתים

**הניסוח הראשון של הממצא הזה היה שגוי, וזה חלק ממנו.** כתבתי ש-`mpremote cp`
"החזיר 0 ובכל זאת כתב קובץ ריק" — **מעולם לא קראתי את קוד היציאה שלו.**
הפולבק שכתבתי היה:

```python
ok = restore_main(port, DEVICE)
if not ok:
    subprocess.call([... cp main.py])     # קוד היציאה לא נבדק
    subprocess.call([... reset])
    print("restore retried")              # ומדווח הצלחה ללא תנאי
```

כלומר: לא ידוע אם ה-`cp` נכשל בשקט או הצליח וכתב אפס. מה שכן ידוע הוא
שהדיווח שלי היה נכון-בצורתו וריק מתוכן, בדיוק כמו הפלט שהוא תיאר.

**ומה שגרוע יותר:** `restore_main` המקורי נכשל **בקול** והדפיס את פקודת
השחזור המדויקת. הפולבק שלי "שיפר" אותו לכדי הצלחה שקטה. לוח עם `main.py`
של אפס בתים נראה **זהה** ללוח בלי `main.py` — REPL עירום ומסך שחור — ואין
שום אזהרה בשום מקום.

**נצפה פעמיים**, שתיהן אחרי שהלוח היה במצב רע: פעם אחת אחרי
`unexpected read during raw paste`, ופעם שנייה אחרי
`Guru Meditation Error (LoadStoreAlignment)` שפרוב שלי גרם. ההשערה: ההעתקה
רצה מול לוח שמאתחל. לא אומת מעבר לכך.

**התיקון (בוצע):** `run_common.restore_main` קורא עכשיו את הגודל **מהלוח**
ומשווה למקור, ומנסה שוב פעם אחת. קוד היציאה של `cp` מדווח על צד המארח, וצד
המארח אינו המקום שאליו הקובץ הגיע.

---

## כלל — כל דבר שנוגע בלוח נכנס ל-`hwtest_ui` או לרץ קיים

**זו הפעם השלישית שפרוב אד-הוק השאיר את הלוח במצב רע:** p25 שנתקע,
p38 שהפיל את הלוח ב-`display.init()` מה-REPL, ו-p40 שסיים ב-Guru Meditation
והשאיר `main.py` ריק. אחרי השני כתבתי "עובר למסלול הבטוח, הבדיקה נכנסת
ל-hwtest_ui" — ואז כתבתי עוד שניים.

הרצים הקיימים כבר פותרים את זה ונבדקו: `clean_board` לפני, `finally` סביב,
`restore_main` מאומת אחרי. פרוב חד-פעמי הוא בדיוק המקום שבו אין למי לתפוס
אותך — ולכן הוא לא חוסך זמן, הוא רק מזיז את העלות אחרי הקריסה.

**מדידה שדורשת את הלוח היא בדיקה. אם היא שווה להריץ, היא שווה לשמור.**


---

## 47 — הפסים שנשארים: פרוב מפריד  · **פרוב בלבד, בלי תיקון**

### ההשערה המובילה, ולמה היא גוברת על שלי

אברהם: *"אני לא מוצא הבדל בין השורות שהוא מצייר לשורות שלא — זה לא שהן
ריקות. חשבתי אולי כשהוא צייר את השורה הזאת תהליך אחר הפריע לו."*

**הנימוק העובדתי: לפסים אין מיקום קבוע.** גבולות באפר הם גיאומטריה, והיו
נותנים פסים באותם גבהים בכל פעם. מיקום שזז מצביע על **תזמון**.

וזה מתחבר למה שכבר נמדד: LVGL, המוח והתזמון חולקים לולאת asyncio שיתופית
אחת. `brain.create` חוסם 840ms, שמירת רג'יסטרי ~350ms, פרץ join 1.75s,
ו-`main.py` עצמו קובע ~89ms כסף שממנו פריימים נכנסים מתחילים ליפול. הפולינג
רץ כל 3 שניות עם 5–6 דיספאצ'ים, מול ציור של 115ms. זה גם מסביר "כמעט תמיד".

### הפרוב — הפרדה בחיסור

`run_hwtest_ui.py --probe-striping` מריץ
`hwtest_ui.probe_screen_transition_striping()`: שני מסכים **עמוסים** (כותרת,
ארבע שורות, סרגל תחתון, ניגודיות מרבית) ו-20 החלפות ביניהם.

**התנאי הדומם מגיע בחינם:** הרץ מוחק את `main.py` לפני החליפה, ולכן בזמן
הפרוב אין מוח, אין פולינג ואין תזמון על הלוח כלל. שום דבר לא יכול להפריע
למעבר חוץ מ-LVGL עצמו.

| מה נראה | מה זה אומר |
|---|---|
| **אין פסים בדומייה** | אברהם צדק — התנגשות. הכיוון: מה רץ בזמן מעבר, לא איך מעבר מצויר |
| **יש פסים בדומייה** | זה בציור, וההשערה על invalidation חוזרת. ההמשך: `--invalidate 1` ואז `2` — עם שני framebuffers, invalidate יחיד נוחת רק במעברים לסירוגין, ולכן "אחד מתקן חצי, שניים מתקן הכל" הוא חתימה |

### שני דברים שהפרוב עושה כדי לא לשקר

**סרגל על הזכוכית.** העין היא המכשיר כאן, ומכשיר בלי סקאלה מחזיר "כמה פסים,
די דקים". הסרגל בשוליים השמאליים מסומן כל **48 פיקסלים** — זה באפר הציור
החלקי של הפאנל (`display.py` משאיר את ה-framebuffers לא-מוגדרים, הדרייבר
מקצה ~1/10 מסך, ו-1/10 מ-800×480 בשורות מלאות הוא בדיוק 48). אם גובה פס
יוצא 48 או 96 — הגיאומטריה חוזרת למשחק למרות המיקום הזז. אם הגבהים
שרירותיים — תזמון.

**אין הדפסה בין ההחלפות.** תעבורת קונסולה על הקו הטורי בזמן מעבר היא בדיוק
סוג ההפרעה שנבדקת כאן; מכשיר שמשבש את מה שהוא מודד עונה על עצמו. מספר
ההחלפה יושב **על המסך**, והתזמונים מודפסים בסוף — כך שדיווח של העין ("3, 7,
12") מצטלב עם חריגות זמן ונהיה נתון.

### גבולות

`PROBE_DONE` אומר שהפרוב **רץ**, לא שהפאנל נקי. אין בהרצה הזאת pass או fail —
הממצא הוא מה שנראה. `invalidate()` מותר (אינו אנימציה ואינו גלילה);
**אנימציית מעבר אסורה** ב-CLAUDE.md וגם הייתה מסתירה את הסימפטום.

**כשיבוא תיקון** — הוא נכנס ל**פונקציית עזר אחת** שכל קוראי `screen_load`
עוברים דרכה (~15 אתרים). תיקון פר-אתר הוא עוד רשימה מתוחזקת ביד, והמחלקה
הזאת כשלה כאן שלוש פעמים: רשימת הקודפוינטים ב-38, רשימת הגזימה ב-45, ורשימת
ה-PAYLOAD לפני כן.


---

## 47 — התוצאה: 0 מתוך 20. ההשערה של אברהם אושרה בחיסור

**נמדד 2026-08-26**, `run_hwtest_ui.py --probe-striping`, לוח דומם
(אין מוח, אין פולינג, אין תזמון — `main.py` מחוק על ידי הרץ).

```
 1-5   151.4  113.7  111.0  110.4  110.7
 6-10  110.9  109.9  110.8  110.7  113.2
11-15  122.5  111.9  112.9  110.9  110.9
16-20  111.9  110.9  112.1  111.4  112.8
מהיר 109.9   חציון 111.4   איטי 151.4   חריגות מעל פי-1.5: אין
```

**העין: אפס שאריות ב-20 מתוך 20.** התזמונים והעין מסכימים — 151.4 היא
הציור הקר הראשון, וזהו. החציון 111.4 מאשר עצמאית את 114.8 ממדידת 42.

אחידות כזאת היא בדרך כלל סיבה לחשוד במכשיר (CLAUDE.md). כאן היא **הסימן**:
אותם שני מסכים, אותה כמות ווידג'טים, ואיש לא מפריע. קפיצות היו הממצא.

**המסקנה:** מסלול הציור נקי בפני עצמו. השארית במוצר מגיעה ממשהו **אחר
שרץ**. ההשערה על invalidation יורדת, ו-`--invalidate 1/2` מיותר.

### ⚠️ הסייג — חיסרתי שני דברים, לא אחד

הפרוב הסיר את המוח, הפולינג והתזמון — **וגם** את שכבת הריאקטיב. המסכים בו
בנויים סטטית: אין `effect()`, אין signals, אין `clean()` ובנייה מחדש.

לכן התוצאה תקפה במלואה ל**מסכים מטמון** (`pages.page_open` טוען מסך בנוי),
ולא נבדקה עבור המסכים שנבנים מחדש בכל פתיחה (`room_page`, `device_page`,
`add_device_page`). זו הפרה של "מוטציה אחת בכל פעם" בצד החיסור, והיא נרשמת
כאן ולא מתורצת.

---

## 48 — פסים "כמו דף מחברת" בלחיצה על כרטיס הבית  · **תצפית, לא מאומתת**

אברהם, אגב 47: *"כשאני לוחץ על הבוקס של הבית שלי במסך הראשי **תמיד** נהיים
לו פסים כמו של מחברת."*

**זה ליד טוב יותר מ-42, משלוש סיבות:**

1. **הוא ניתן לשחזור** — "תמיד", לעומת שארית שמופיעה לפעמים.
2. **הוא ממוקד** — ווידג'ט אחד, אינטראקציה אחת, בלי מעבר מסך בכלל.
3. **הדפוס גיאומטרי** — "כמו דף מחברת" הוא קווים במרווחים **שווים**. זה
   ההפך מהפסים של 42, שמיקומם זז. מרווח קבוע מצביע חזרה על באפר הציור
   החלקי (~48 פיקסלים), הפעם כתפרים בין נתחי flush.

לחיצה על כרטיס משנה סגנון ⇒ invalidate של אותו אובייקט ⇒ ציור מחדש חלקי.
`ui_home` הוא מסך מטמון, כלומר בדיוק המקרה ש-47 **כן** מכסה — ושם הציור יצא
נקי. ההבדל: ב-47 הלוח היה דומם, וכאן `main.py` רץ.

**הפרוב הבא (לא הורץ, לא אושר):** לבנות את `ui_home` האמיתי על לוח דומם,
להחיל `lv.STATE.PRESSED` על כרטיס הבית ולצייר — 20 פעם.

* פסים בדומייה → זה בציור החלקי, ומדידת מרווח הקווים מול 48 מכריעה.
* אין פסים בדומייה → שוב התנגשות, ואז יש **שני** סימפטומים עם אותו שורש
  ואחד מהם ניתן לשחזור לפי דרישה.

**למדוד את מרווח הקווים בשני המקרים.** אם הוא 48 — הגיאומטריה חוזרת.

---

## 48 — הפרוב: `--probe-home-press`  · **פרוב בלבד, בלי תיקון**

`run_hwtest_ui.py --probe-home-press` בונה את `ui_home` האמיתי על לוח דומם,
מוצא את כרטיס הבית, ומחיל עליו `lv.STATE.PRESSED` ומסיר אותו — 20 פעם. אותה
הפרדה בחיסור כמו ב-47: הרץ מוחק את `main.py`, ולכן בזמן שהכרטיס לחוץ אין מוח,
אין פולינג ואין תזמון על הלוח.

`_run_probe` בצד המארח קיבל את הקריאה כפרמטר במקום להיות כתוב סביב פרוב אחד.
עותק שני של "חפש `PROBE_DONE`" הוא בדיוק הרשימה המתוחזקת ביד שכבר עלתה כאן
שלוש פעמים — ובפסק הדין עצמו זה המקום הגרוע ביותר לעותק.

### הכרטיס נמצא לפי מה שהוא עושה, לא לפי מקומו בעץ

`scr.get_child(1).get_child(0)` נכון היום, ויצביע בשקט על משהו אחר ביום
שתיכנס שורה מעליו — ופרוב שמכוון לווידג'ט הלא נכון מדווח "אין פסים" בביטחון
מלא. במקום זה הפרוב סורק את העץ ואוסף כל ווידג'ט שצבע הרקע שלו **משתנה** תחת
`PRESSED`, מדפיס את כולם, ובוחר את הגדול. רשימה ריקה עוצרת את הריצה: זו בדיוק
השאלה שהפרוב בא לבדוק, ולכן החיפוש הוא גם בדיקת המכשיר.

### מה מצופה, ובאיזה טווח — נכתב לפני ההרצה

הגיאומטריה **נגזרת** מ-`ui_home.py` + `theme.py`, לא נמדדת על הזכוכית: body
ב-y=110 בגובה 336 עם pad 18 ⇒ תיבת תוכן 764×300 ב-(18,128); hero הוא 3 מתוך 5
של 300−14 ⇒ **764×171 ב-(18,128)**.

| מה | צפי | ומה יהיה חשוד |
|---|---|---|
| קופסת הכרטיס | `18,128 .. 781,298` | ערך אחר = קראתי את הפריסה לא נכון, וכל השאר בטל |
| ציור מחדש ללחיצה | **20–60 ms** (34% מהמסך × 111.4ms מ-47 ⇒ ~38) | מעל 100 = כל המסך מצויר מחדש בשביל לחיצה, וזה ממצא בפני עצמו. מתחת ל-5 ואחיד = לא צויר כלום, לבדוק את הפרוב לפני שמאמינים לו |
| מרווח הפסים, **אם** הם תפרים | **50px**, ו-**3** תפרים בלבד | — |

**תיקון לסעיף שלמעלה:** הוא כתב "אם הוא 48 — הגיאומטריה חוזרת". 48 הוא הפיץ'
של אזור ברוחב **מלא** (38400px של באפר חלקי ÷ 800). הכרטיס רחב 764, ולכן LVGL
חותך אותו ל-38400÷764 = **50** שורות. הסרגל של 47 מסומן ב-48 כי שם נמדדו מסכים
שלמים; כאן המספר הנכון הוא 50.

ומכאן ממצא שקדם למדידה: **3 קווים במרווח 50 אינם "דף מחברת".** אם מה שיופיע
הוא הרבה קווים צפופים, השערת התפרים בצרות עוד לפני שמדדנו משהו.

### הסרגל

הסרגל של 47 יורד בקצה השמאלי של המסך; כאן הוא לא משרת, כי הכרטיס מתחיל 18px
משם ורץ 764px, ולכוון גובה של פס לרוחב שלושה רבעי הזכוכית זו לא מדידה. לכן
הסקאלה יושבת **על** הכרטיס, בתוך המלבן שמצויר מחדש — היא נצבעת בפסים יחד איתו,
וזה גם העניין: סרגל מחוץ לאזור לא יכול לומר איפה **בתוכו** נפל פס.

מסומן כל 10px עם תווית כל 50, והמספרים הם היסט מראש הכרטיס. מספרים עגולים ולא
הפיץ' החזוי — סרגל שמסומן בחיזוי מזמין את העין להסכים איתו. ש-50 הוא גם החיזוי
נאמר במפורש במקום להיות מוסתר בין השנתות.

מיקום הסרגל **נמדד ומתוקן**: `set_pos` יחסי לאזור התוכן של ההורה ו-`box` הוא
קואורדינטות מסך. הפרוב מניח אותו באפס, קורא איפה זה נחת, ומתקן בהפרש — במקום
להניח שלמסך אין padding ולהזיז כל שנתה בדיוק בכמות שאיש לא היה שם לב אליה.
מיקום תווית 0 מודפס מול ראש הכרטיס, כך שהתיקון נבדק ולא נסמך עליו.

### מה הפרוב מחסיר **חוץ** מ-`main.py` — נרשם לפני התוצאה

47 נאלץ לרשום את זה נגד עצמו אחרי המעשה. כאן זה מראש:

* **אין מגע אמיתי.** ה-state נקבע מקוד, ולכן ה-GT911 לא נקרא ב-I2C בזמן הציור
  ואין עיבוד indev.
* **אין קליק.** בלחיצה האמיתית רץ מיד אחריה `pages.page_open`, שבפעם הראשונה
  בונה את `rooms_page`. כאן לא.
* **אין pump.** `lv.refr_now` מצייר בקריאה אחת; המוצר מצייר מ-`timer_handler`
  של `lvgl_loop` בין משימות אחרות.

כל אחד מהשלושה יכול להיות המפריע. ולכן "אין פסים" כאן מנקה את **הציור**, לא את
הלחיצה במוצר.

### גבולות

כמו ב-47: `PROBE_DONE` אומר שהפרוב **רץ**. אין pass ואין fail — הממצא הוא מה
שנראה. הציור הקר הראשון הוצא מהלולאה במפורש (ב-47 הוא נחת על החלפה 1 והופיע
כ-151.4 בין 111-ים), כך שלחיצה 1 היא לחיצה ככל השאר.

---

## 49 — התוצאה: ציור יחיד מפספס, ציור כפול נקי. 10 מתוך 10

**נמדד 2026-08-27**, `--probe-home-press`, לוח דומם. לחיצות אי-זוגיות מציירות
את הכרטיס פעם אחת, זוגיות פעמיים, לסירוגין בתוך אותה הרצה.

אברהם: *"הפסים נדלקו ונכבו בכל החלפה והם מתאימים בדיוק לשרטוט."*

### בדיקת המכשיר קדמה לעין, ועברה

| | single | DOUBLE | הפרש |
|---|---|---|---|
| לחיצה | 86.6 | 170.8 | +84.2 |
| שחרור | 39.3 | 120.5 | +81.2 |

המעבר הנוסף עולה ~82ms בשני המקרים. הוא צייר. ההשוואה על הזכוכית תקפה.

### מה מוכח

**הזכוכית מציגה תוכן שהציור הנוכחי לא ייצר, וציור שני של אותו אזור מספק אותו.**

זו האמירה המדויקת, והיא נשארת נכונה. **המסקנה שנבנתה עליה — שהתמונה נשמרת
ביותר ממקום אחד — הופרכה; ראה 50 ו-51.** השורה הראשונה היא תצפית, השנייה
הייתה פרשנות, והפרשנות היא שנפלה. הן נשארות מופרדות כאן בכוונה.

שלושת התיאורים אכן נופלים מהתצפית בלי שארית — כפתור גדול, מרווח קבוע, ושורה
ששורדת למסך הבא — אבל ההסבר להם הוא שורה שלא נכתבה, לא מחסן שני. 51.

**ו-47 אישרה את זה, לא סתרה.** שם כל החלפה ציירה מסך **שלם**, כלומר כל פיקסל
נכתב וכל המחסנים יצאו נכונים. ציור מלא חסין לבאג הזה מעצם הגדרתו. 0 מתוך 20 הוא
בדיוק מה שכפל-באפר חוזה — קראתי את התוצאה כזיכוי של מסלול הציור, וזו הקריאה
השגויה השנייה שלי באותה משימה. הראשונה: "מיקום שזז ⇒ תזמון". גם היא נופלת —
עם שני מחסנים, איזה תפר נראה תלוי בהיסטוריית ההחלפות, ולכן **גיאומטריה מייצרת
מיקום שזז**.

### שני סייגים

1. **ההרצה לא הייתה עיוורת.** התווית על המסך אמרה `single` / `DOUBLE`. אפקט
   בינארי על פני 10 זוגות אינו מה שהטיית ציפייה מייצרת בקלות, אבל זה פגם והוא
   נרשם. גרסה מעוורת (סדר אקראי, תווית מוסתרת) תסגור אותו אם יידרש.
2. **האסימטריה לחיצה/שחרור הייתה באג בפרוב, כפי שנחזה.** ראה 50 — שם היא גם
   מוסברת מכנית.

### הפרוב הזה הוא מבחן הקבלה לתיקון

אחרי שינוי בשכבת ה-framebuffer, אי-זוגיות וזוגיות חייבות להיראות **זהות
ושתיהן נקיות**. תיקון שלא עובר את זה לא עבר.

---

## 50 — שתי השערות שנפלו במדידה, ולמה הן רשומות

לפני התיקון האמיתי, שתי מסקנות שלי נבדקו על חומרה ו**הופרכו**. שתיהן נשארות
כאן, כי גרסה קודמת של הסעיף הזה כבר הכריזה על הראשונה כשורש.

### א. "המחסן השני הוא ה-framebuffer השני" — הופרך

`rgb_bus.c` מגדיר `flags.double_fb = 1` (שורת upstream), ומשימת ההעתקה
מסנכרנת את הבאפר השני ב-`memcpy` של 768,000 בתים אחרי כל רענון. נראה משכנע.

נספר על הקושחה הצרובה (`--probe-display-buffers`): חיזוי ‎1,721,600, נמדד
**‎1,731,328** — פער 0.6%; region 2 לבדו נתן ‎1,536,008 = 2 × 768,000 + 8.
שני framebuffers, מאומת.

נבנתה קושחה עם `double_fb = 0`. הספירה ירדה ל-**‎963,324** (‎-768,004 בדיוק,
ה-bounce נשאר). **והפסים לא זזו.** ההשערה מתה.

מה שכן נמדד ונשאר נכון: בלי הבאפר השני ה-`memcpy` לא רץ, ולחיצה ירדה
**‎87ms → ‎40ms**. במקום זה קיבלנו **קרע** גלוי — "המסך יורד מלמעלה".

### ב. "זוג באפרי הציור של LVGL" — הופרך

השערה של אברהם, ומבוססת: LVGL מקבל שני draw buffers של ‎76,800 בתים ומחליף
ביניהם. `--single-draw-buffer` (שינוי Python, בלי צריבה) נתן באפר אחד —
מאומת בפלט: `_frame_buffer2 None`. **הפסים לא זזו.** מחיר: ‎3.4ms בלבד, לא
ה-‎20–40 שחזיתי.

בשלב הזה לא נשאר שום באפר כפול בשום מקום, והציור הכפול עדיין ניקה.

---

## 51 — השורש: אוף-ביי-וואן ב-`rotate0`. **תוקן ואומת**

הבדיקה שהכריעה היא זו שנכתבה כ"המשך המתוכנן" עוד ב-48 ולא הורצה:
**לשנות את רוחב האזור המצויר.**

`--probe-home-press --vary-width` — לחיצות אי-זוגיות על כרטיס ברוחב ‎764,
זוגיות ברוחב ‎382. אם הפסים הם תפרים בין רצועות, הפיץ' הוא `38,400 ÷ רוחב`:

| רוחב | גובה רצועה | תפרים |
|---|---|---|
| 764 | 50 | 3 |
| 382 | 100 | **1** |

אברהם: *"כשזה חצי הפסים נשארים מאחוריו חוץ מאחד שעולה עליו."* **אחד.**
והשאר — רקע שהכרטיס פינה ואיש לא צייר מחדש.

### הקוד

`rgb_bus_rotation.c`, `rotate0`, המסלול שרץ כשהאזור **אינו** ברוחב מלא:

```c
for (uint32_t y = y_start; y < y_end; y++) {   // y_end הוא כולל!
    memcpy(dst, src, src_bytes_per_line);
```

`y_end` הוא `area.y2` של LVGL, כלומר **כולל**. הלולאה מעתיקה שורה אחת פחות,
ו**השורה האחרונה של כל רצועה לעולם לא נכתבת** — היא שומרת את מה שהיה שם.

שתי שורות מעליה, במסלול המהיר: `(y_end - y_start + 1)`. **עם ה-+1.**

### מה זה מסביר

| תצפית | הסבר |
|---|---|
| פסים במרווח קבוע | שורה חסרה בכל גבול רצועה |
| המרווח עוקב אחרי הרוחב | גובה רצועה = ‎38,400 ÷ רוחב |
| **47 נתן 0 מתוך 20** | מסך מלא ⇒ המסלול המהיר ⇒ נכון |
| כפתורים קטנים נקיים | רצועה אחת ⇒ החסרה היא השורה התחתונה, על הגבול |
| שארית שעוברת למסך הבא | שורות שאיש לא כתב, בכל מקום שלא צויר מחדש |

**ואישור מהקוד עצמו:** בענף המסובב, `copy_pixels` עושה
`y_end += 1; // removes black lines between blocks`. **upstream פגש את
הסימפטום, קרא לו בשמו, ותיקן רק את הצד המסובב.** רוטציה 0 לא קיבלה אותו.

### קצה פתוח שלא נסגר

זה **לא** מסביר למה ציור כפול ניקה — אותו אזור, אותה חלוקה, אותן שורות אמורות
ליפול שוב. אין לי תשובה, והיא לא הומצאה. התיקון אומת עצמאית בעין.

---

## 52 — התצורה הסופית, ומה נמדד בכל שלב

`double_fb` הוחזר ל-**1**: הפסים מעולם לא נבעו ממנו, והוא מה שמבטל את הקרע.
ואז ה-`memcpy` של 768KB חזר איתו — אז הוא הוחלף ב**תיבה חוסמת**: משימת
ההעתקה צוברת את המלבן של הרצועות ומסנכרנת רק אותו.

| | לחיצה | שחרור | DOUBLE | פסים | קרע |
|---|---|---|---|---|---|
| מקורי | 86.6 | 39.3 | 170.8 | יש | אין |
| `double_fb=0` | 39.8 | 39.5 | 76.2 | יש | **יש** |
| + תיקון השורה | 40.0 | 39.4 | 77.2 | **אין** | יש |
| + `double_fb=1` | ~87 | — | — | אין | **אין** |
| **+ תיבה חוסמת** | **40.5** | **39.1** | **102.2** | **אין** | **אין** |

עמודת ה-DOUBLE היא הבדיקה שהתיבה מעתיקה **מספיק**: בלי סנכרון 76.2, תיבה
102.2, מסך מלא 170.8. ‎26ms הם שליש מ-‎94 — היחס בין ‎261KB ל-‎768,000.
תיבה ריקה הייתה נוחתת על 76.

**‎88 מתוך 88 בסוויטת ה-UI** על הקושחה החדשה.

### מה שנרשם נגד עצמי

* **הרצתי צריבות לפני שהרצתי בדיקה חינמית.** בדיקת הרוחב נכתבה כהמשך מתוכנן
  ב-48 והגיעה רק אחרי שתי בניות ושתי צריבות. שתי ההשערות שנפלו היו נחסכות.
* **הריצות המזווגות לא היו עיוורות** — התווית על המסך אמרה מה צפוי.
* **חיזוי המהירות של הבאפר היחיד היה ‎20–40ms; נמדד ‎3.4.**
* **הקריטריון שרשמתי מראש ל"התיבה מעתיקה מדי מעט" (‎~40ms) היה שגוי.**
  הלחיצה משלמת על הסנכרון של הציור **שקדם** לה, לא של עצמה.

---

## 53 — מהירות ורגישות המגע

**תקציב הנגיעה, ולמה עבודת הרינדור לא הורגשה:** הקליק של LVGL יורה על
ה**שחרור**, כלומר כל הקשה עולה שתי דגימות ולא אחת.

`lv_conf.h` משאיר את `LV_DEF_REFR_PERIOD` על **33ms** ⇒ עד ‎66ms רק דגימה,
יותר מכל ‎47ms שנחסכו בציור. `display.TOUCH_SAMPLE_MS = 10` מכוון את הטיימר
של ה-indev בזמן ריצה. ‎10 ולא פחות: ה-pump משרת את LVGL כל ‎~10ms, וטיימר לא
יורה מהר יותר מהמנוע שמריץ אותו.

### הרגיסטרים של ה-GT911 — לפי המדריך של Goodix

| רגיסטר | הגדרה |
|---|---|
| `0x804F` `Shake_Count` | דיבאונס: חצי-בית לשחרור, חצי-בית ללחיצה |
| `0x8053` / `0x8054` | סף לזיהוי נגיעה / לזיהוי הרמה |
| `0x8056` `Refresh_Rate` | *"Coordinates report period: 5+N ms"* |

ההשהיה היא **מכפלה**: מספר סריקות × תקופת דיווח.

**נקרא מהלוח, לא נוחש** (`--probe-touch`): `press 80`, `leave 50`,
`shake 0x02`, `refresh 5` (‎10ms), ו-`Noise_Reduction` **וגם** `Filter` שניהם
על **המקסימום** — הפאנל הזה כויל נגד רעש, ולכן הסף לא הורד עד הסוף.

נכתב ואומת בקריאה חוזרת:

| | היה | עכשיו |
|---|---|---|
| `press_level` | 80 | **65** |
| `leave_level` | 50 | **45** |
| `shake_count` | 0x02 | **0x01** |
| `refresh_rate` | 5 (‎10ms) | **3 (‎8ms)** |

```
דגימת LVGL    33ms → 10ms
דיבאונס       20ms →  8ms
דגימת LVGL    33ms → 10ms
              ────   ────
               86ms → 28ms
```

אברהם: *"עובד מעולה."*

### ⚠️ הערכים האלה אינם בגיט

הם בפלאש של **בקר המגע**, ושורדים כיבוי, צריבה ו-`--erase-all`. שום דבר
בריפו לא רואה אותם. **דרך החזרה: `80 / 50 / 0x02 / 5`.**

```
run_hwtest_ui.py --probe-touch --press-level 80 --leave-level 50 \
                 --shake-count 2 --refresh-rate 5
```

לפלאש הזה יש מספר מוגבל של מחזורי כתיבה — זה מעשה בודד ומכוון, לא לולאת
כיול. אם יופיעו נגיעות רפאים ⇒ הסף; קליקים כפולים ⇒ `shake_count`.

---

# מה שנשאר — נרשם 2026-08-27 בסוף היום

48 ו-42 סגורות. מה שלהלן הוא **כל** מה שפתוח, לפי סדר סיכון.

---

## 54 — ✅ נסגרה 2026-08-27 · `flash.py`

**הייתה P0 — היחידה ברשימה שעלתה נתונים של משתמש אמיתי.**

`products/panel/host/flash.py` עושה את הרצף שלם: גיבוי `/data` מאומת לפי גודל,
ארכוב הבינרי, צריבה, `deploy.ps1`, החזרה מאומתת. **גיבוי שלא תואם = סירוב
לצרוב.** ההשוואה (`run_common.compare_listings`) היא פונקציה טהורה, ו-
`tests/test_panel_data_backup.py` מפעיל אותה על הכשלים שחשובים — קובץ שלא הגיע,
קובץ קטוע, קובץ קצר, קובץ עודף, והכול נעלם בבת אחת — במקום לראות רק את המסלול
המוצלח על הספסל.

**אומת על הלוח החי בלי לצרוב ובלי למחוק:** מסלול הסירוב (`:/nosuchdir` ⇒
`None`), המסלול המוצלח (13 קבצים מגובים ומאומתים), **ומוטציה אחת** — קיצוץ קובץ
מגובה לאפס בתים נתפס: `settings.json is 0 bytes, expected 119`.

התיאור המקורי, לתיעוד:

`deploy.ps1` פורס **קבצים**. הוא לא צורב **קושחה**. אין בריפו שום סקריפט
שצורב — היום זה נעשה שלוש פעמים ביד:

```powershell
python -m esptool --chip esp32s3 -p COM7 -b 460800 write-flash --erase-all 0x0 <bin>
```

`--erase-all` מוחק את ה-VFS **כולו**, ובתוכו `/data`: מכשירים, אזורים,
תזמונים, הגדרות ו-journal. שלוש פעמים היום זה נזכר בזמן. **פעם רביעית לא
חייבת להיזכר.** אין שום דבר במערכת שיעצור את זה, ואין שום סימן אחר כך — הלוח
פשוט עולה ריק, וזה נראה כמו לוח חדש ולא כמו אובדן.

**מה שצריך:** סקריפט צריבה שעושה את הרצף שלם ובסדר הנכון —
לגבות `:/data` למארח, לאמת שהעותק לא ריק, לגבות את הבינרי שעומד להידרס
(`~/lvgl_micropython/build/*.bin` הוא **עותק יחיד** וכל בנייה דורסת אותו),
לצרוב, `deploy.ps1`, להחזיר `/data`, ולאמת גדלים מול הלוח.

**"אימות גדלים" ולא "קוד יציאה"** — משימה 46 כבר לימדה שהעתקה מדווחת הצלחה
ומשאירה קובץ של 0 בתים. `run_common.restore_main` כבר עושה בדיוק את זה עבור
`main.py`; `/data` לא קיבל את אותו יחס.

**מוכן כשעברה מוטציה:** למחוק `/data` מהלוח ולוודא שהסקריפט מסרב להמשיך.

---

## 55 — ❌ בוטלה. לא הייתה קיימת מעולם

**נכתבה ונסגרה באותה שעה, 2026-08-27.** נרשמת כאן במלואה כי אופן הכשל חשוב
יותר מהמשימה.

טענתי ש-`.gitattributes` חסר ושהפקודה `bash firmware/h2_coordinator/host_test/run.sh`
מעולם לא רצה. **שתי הטענות שגויות.** `.gitattributes` קיים מ-2026-08-20 עם
`*.sh text eol=lf` ועם הסבר מלא; הבלוב תמיד היה LF; ו-`git check-attr` מאשר
`text: set`, `eol: lf`.

והבדיקה שהייתה צריכה להיות ראשונה — **להריץ את הפקודה** — נותנת:

```
1247 checks, 0 failures
```

בדיוק המספר ש-CLAUDE.md מצטט.

### איך זה קרה, ולמה זה שייך לכאן

**א. לקחתי חסם מטבלה מיושנת** ("חסמים עומדים") בלי לאמת. הטבלה תוקנה עכשיו.

**ב. ואז "אימתתי" אותו עם מכשיר שבור.** ‏`grep -c $''` על הקובץ החזיר
**13** — ונראה כמו ספירת שורות CRLF. ‏13 הוא **מספר השורות בקובץ**. הקובץ
מכיל **אפס** בייטי CR (`od -c`, וספירה ב-Python: `CR total: 0`).

זו בדיוק המחלקה שכתובה ב-CLAUDE.md: *"המספר הוא נגזרת של קוד המדידה"*, ו-
*"אם אפשר לחזות את התוצאה מקריאת הבדיקה בלי להריץ אותה — היא לא מדדה כלום"*.
מספר שיוצא **שווה בדיוק** לגודל אחר בבעיה הוא סימן אזהרה, לא אישור.

**המסקנה המעשית:** לפני שכותבים משימה מטבלת סטטוס — **להריץ את הדבר**. כאן
זה עלה שנייה אחת והיה מבטל את המשימה לפני שנכתבה.

---

## 56 — מחיצת האפליקציה מלאה ב-99.9%

**חומרה: P2. לא כואב היום, חוסם את השינוי הבא.**

הבנייה של 2026-08-27 הזהירה:

```
micropython.bin binary size 0x2d5530. Smallest app partition is 0x2d6000.
0xad0 bytes (0%) free.
Warning: The smallest app partition is nearly full!
```

**‎2,768 בתים.** התיקונים של היום הוסיפו 32 בתים והספיקו בקושי. זה **לא**
נגרם מהם — זה היה כך גם בקושחה מיולי.

**המשמעות:** התוספת המשמעותית הבאה לקושחה — פונט, דרייבר, מודול מוקפא —
פשוט לא תיכנס, והבנייה תיכשל ב-"does not fit".

**מה שצריך:** טבלת מחיצות עם מגירת app גדולה יותר. הפלאש הוא 8MB ויש מקום.
**לפני** שנצטרך אותה, לא באמצע חקירה.

---

## 57 — ✅ נסגרה 2026-08-28 · `fonts/README.md`

**הייתה P2.**

43 הופיעה בתדריך של אברהם ולא בשום קובץ. חמשת ה-`.bin` ב-
`products/panel/device/fonts/` נצרבים ללוח בכל deploy, **ואין תיעוד איך הם
נוצרו** — איזה כלי, איזה טווח קודפוינטים, איזה גודל, מאיזה TTF.

נמדד שכל ה-ASCII נוכח בכל החמישה (`test_font_glyph_coverage` בסוויטה), אז
אין באג פתוח. **הסיכון הוא שחזור:** אם יידרש גליף חדש, אף אחד לא יודע לבנות
מחדש.

זו אותה מחלקה בדיוק שמשימה 54 עוסקת בה ושהפאץ' ל-C היה בה עד היום — **ידע
תפעולי שחי רק בראש של מישהו.**

**המתכון לא נשאל ולא נוחש — הוא היה בהיסטוריה של הריפו.** ‏`lv_font_conv`
חותם את שורת הפקודה שלו בכותרת כל `.c` שהוא פולט, ו-`d416913` (עידן קושחת ה-C)
נושא חמש כאלה. מה שנמצא שם הוא `--format lvgl`, כי הוא בנה מערכי C ולא את
ה-`.bin` שנטענים היום — ולכן זה שוחזר והורץ, לא הועתק.

`products/panel/device/fonts/README.md` מתעד את הפקודה, ו-`fonts/src/` מוונדר
את שני ה-TTF ואת הרישיון. **הקלט הוא חלק מהמתכון:** פקודה שהקבצים שהיא אוכלת
אינם בנמצא היא תיעוד, לא ביטוח — ו-Assistant הסטטי כבר לא קיים ב-`main` של
`google/fonts`, שמפיץ היום פונט משתנה בלבד.

**מוכח, לא מתועד:** בנייה מחדש מ-`src/` מייצרת את חמשת הקבצים **זהים
בייט-בייט** (5340 / 7512 / 13484 / 13804 / 37752). בנפרד, פרסינג של הכותרות
של ה-`.bin` הנצרבים מסכים על כל פרמטר: `bpp=4`, דגל דחיסה 0, והחמישה הדלילים
ב-`cmap` הם בדיוק U+05F3 U+05F4 U+2013 U+2014 U+20AA, אחד לאחד מול ארבעת
ה-`-r` הקטנים.

**המוטציה:** הפלת `-r 0x20AA` לבדה נותנת 5288 במקום 5340 ו-`cmp` נכשל — כלומר
ההשוואה חיה ולא עוברת על הכול. חמישה מתוך חמישה זהים הוא בדיוק סוג התוצאה
ש-CLAUDE.md מורה לחשוד בה, ולכן היא נבדקה ולא נחגגה.

---

## 58 — למה ציור כפול ניקה את הפסים? לא הוסבר

**חומרה: P3. שאלה פתוחה, לא באג.**

התיקון ב-51 (`y <= y_end`) מסביר את הפסים במלואם ואומת. הוא **לא** מסביר את
התצפית שהובילה אליו: ציור שני של אותו אזור ניקה אותם, 10 מתוך 10, פעמיים.

אותו אזור ⇒ אותה חלוקה לרצועות ⇒ אותן שורות אמורות ליפול שוב. אין לי תשובה,
והיא לא הומצאה.

**למה זה נשאר רשום:** או שיש עוד מנגנון שלא ראינו, או שההבנה שלי איך LVGL
מחלק אזור לרצועות חסרה משהו. שתי האפשרויות שוות רישום. אין דחיפות — הסימפטום
נעלם.

---

## 59 — ✅ נסגרה 2026-08-28 · `touch_registers.json`

**הייתה P3.**

`65 / 45 / 0x01 / 3` יושבים בפלאש של ה-GT911. הם שורדים כיבוי, צריבה
ו-`--erase-all`, **ושום דבר בריפו לא רואה אותם.** אם מישהו ישנה אותם, אין
דרך לדעת שהשתנו ואין השוואה מול בסיס.

הישנים — `80 / 50 / 0x02 / 5` — רשומים כאן ובפלט של `--probe-touch`, וזו כל
ההגנה שיש.

**מה שנעשה:** ‏`probe_touch` מדפיס עכשיו שורה אחת קריאת-מכונה
(`TOUCH_CONFIG press_level=… leave_level=… shake_count=… refresh_rate=…`, הכול
בעשרוני), ו-`run_hwtest_ui.py --probe-touch` מנתח אותה לתוך
`products/panel/host/touch_registers.json` **בכל ריצה**. הרץ גם מדפיס מיד אם
הלוח סטה מהבסיס, כדי שמי שעומד בספסל יראה את זה בלי להריץ טסט.

הקובץ מוחזק בשני חצאים שאסור להם להתערבב: ‏`expected` נכתב **ביד** (הכוונון של
53), ו-`observed` נכתב **רק** על ידי הפרוב. אם הפרוב היה מתחזק את שניהם, הטסט
היה משווה קובץ לעצמו ועובר על כל קלט. ‏`previous` הוא דרך החזרה.

**השורה האחרונה מנצחת:** ריצה שכותבת ערכים מדפיסה את השורה פעמיים — פעם למה
שהיה בבקר בהגעה, ופעם למה שיש אחרי השמירה. רק השנייה מתארת את הלוח שעל הספסל.

`tests/test_touch_registers_match_the_baseline.py` (6 טסטים) משווה בין החצאים,
ואוכף גם ש-`leave_level < press_level` בכל שלושת החצאים — האינווריאנט שהפרוב
מסרב לכתוב מעבר לו — ושל-`previous` יש ערכים שונים מ-`expected`, אחרת אין לאן
לחזור.

**מוכח על הלוח החי, לא על הספסל בלבד.** לפני הריצה הטסט היה **אדום** (אין חצי
`observed`); הפרוב על COM8 החזיר `65 / 45 / 1 / 3`, כלומר בדיוק הבסיס; הטסט
נעשה ירוק. **המוטציה:** שינוי `observed.press_level` ל-80 לבדו מפיל טסט אחד
עם ההודעה `press_level is 80 not 65`. הקריאה לא נגעה בפלאש של הבקר — לא הועבר
לה שום ערך כתיבה.

---

## חסמים שלא זזו

| מה | פותח | הערה |
|---|---|---|
| ~~בנייה אחת ב-ESP-IDF~~ | ✅ נסגר 2026-08-23 | קומפל; **19/20/21 compile-verified** |
| **21** — `remove_device` | צריבת ה-H2 | הקוד והאדפטר כתובים; הלוח מריץ 0.11.1 |

## מפגע ספסל, לא משימה

**כבל ה-USB של הלוח נשמט מדי פעם.** קרה פעמיים היום: בתחילת הסשן (COM7 נעלם
ואז חזר לבד) ובאמצע `deploy.ps1` — שם הוא הפיל את הפריסה **לפני** שלב 4 והשאיר
את הלוח בלי `main.py`, כלומר מסך שחור.

ה-CH340 הוא שבב נפרד מה-ESP32 ומוזן מה-USB, ולכן קושחה לא יכולה להפיל אותו.
**זה פיזי.** ההודעה של mpremote — `failed to access COM7 (it may be in use by
another program)` — **מטעה**: אין תהליך תופס, הפורט פשוט לא קיים. לבדוק
`list_ports` לפני שמחפשים יתומים.

באמצע `deploy` זה עולה מסך שחור. באמצע **צריבה** זה יעלה יותר.
