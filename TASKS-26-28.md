# משימות 26–28 — הפער הרב-גאנגי, מקצה לקצה

מסמך אחד, ארבע משימות, סדר מחייב. כללי הברזל של `CLAUDE-CODE-TASKS.md`
חלים כלשונם: משימה אחת בכל פעם, קומיט אחד למשימה, אל תיגע בקבצים
מחוץ להיקף, אמת את הממצא לפני שאתה מתקן, אל תתקדם על אימות שנכשל,
אל תשנה טסטים כדי שיעברו, בלי refactor.

**הפניות הן לקבצים ולסמלים, לא למספרי שורות.** מספרי השורות במסמך
הזה נבדקו על `6168f46` וזזו מאז.

---

## הסדר, ולמה הוא מחייב

```
26א  ← המפתוח פר-endpoint          חוסמת את כל השאר
26ב  ← bind לכל endpoint            אחרי 26א בלבד
27   ← מסך הוסף-מכשיר + זיהוי       אחרי 26א
28   ← זיהוי בנורית                 חסומה בבניית ESP-IDF, לצד 21
```

**26ב לפני 26א** = גלגול רגרסיה קיימת לכל בית. היום היא בלתי-נראית
רק משום שה-`bind` נשלח ל-endpoint אחד, ולכן גאנג 2 לא מדווח ולא
משחית כלום. ברגע ש-26ב תרוץ, כל דיווח מגאנג 2 ידרוס את מצבו של
גאנג 1 — בתצוגה, ב-toggle, ובכלל ההתערבות הידנית.

**27 לפני 26א** = מסך שנותן למשתמש בדיוק את מה שביקש, שבור: שתי
הישויות יקראו מאותו תא, יציגו מצב זהה ושגוי, וה-toggle יישלח הפוך.
וכפתור הזיהוי עצמו ישקר (ראה 26א, השלכה 4).

---

## 26א — `_states` / `_expected` / `read_state` פר-(ieee, endpoint)

**בהיקף:** `src/smart_kosher/adapters/zigbee_gateway.py` בלבד.

### הממצא

`_on_state` כותב `self._states[ieee] = bool(payload["on_off"])` —
מפתח `ieee` בלבד. תא אחד למכשיר פיזי. אותו דבר ב-`_expected`.

**הודגם על חומרה**, בריצה שהוכיחה את משימה 12:

```
GANG2 on / waiting ep1 -> {'confirmed': False, 'observed': True} | reports: [(2, True)]
```

`observed: True` בזמן שגאנג 1 כבוי. הדיווח מגאנג 2 דרס את המצב
שמייצג את גאנג 1. הממתין סירב נכון — התא משקר.

### ארבע השלכות, לא אחת

| # | איפה | מה נשבר |
|---|---|---|
| 1 | `devices()` | התצוגה מראה את מצבו של הגאנג שדיווח אחרון |
| 2 | `_toggle` | קורא מאותו תא ובוחר כיוון — **שולח את הפקודה ההפוכה** |
| 3 | כלל ההתערבות הידנית | `_expected` נושא בדיוק אותו פגם מפתוח |
| 4 | `read_state(ieee)` | מקבל `ieee` בלבד וקורא `entry.get("endpoint", 1)` — **אין דרך לקרוא את גאנג 2 בכלל** |

השלכה 4 היא זו שהופכת את זה מ"תיקון תצוגה" למשהו שמדליק אור
בבית של מישהו ולא יודע להחזיר אותו. כפתור הזיהוי במשימה 27
נשען עליה.

### מה לעשות

`_states`, `_expected`, `_state_ms` ו-`read_state` יתמפתחו לפי
`(ieee, endpoint)`.

**אל תמציא את מקור ה-endpoint — הוא כבר קיים:**

* `_state_ep` — נוסף במשימה 12, נושא את ה-endpoint של הדיווח.
* `confirm_target(entity_id)` — נוסף במשימה 25, מחזיר `(ieee, endpoint)`.
* `_endpoint_admits` — כלל ההשוואה. **תאימות אחורה לדיווח בלי
  endpoint עוברת דרכו, בדיוק כמו במשימה 12. אל תכתוב כלל שני.**

`read_state` מקבל פרמטר endpoint. ברירת מחדל שומרת על הקוראים
הקיימים.

### הוכחה — שלוש מוטציות, כל אחת חייבת להאדים

* **M1:** החזר את `_states` למפתוח `ieee` בלבד. דיווח מגאנג 2 חייב
  להשחית את התצוגה של גאנג 1.
* **M2:** החזר את `_toggle` לקריאה מהתא המשותף. toggle על גאנג 1
  אחרי דיווח של גאנג 2 חייב לשלוח את הכיוון ההפוך.
* **M3:** החזר את `read_state` להתעלמות מה-endpoint. קריאה של גאנג 2
  חייבת להחזיר את מצבו של גאנג 1.

### אימות על חומרה

ה-DUT `70:d0:7e:…` הוא הריג היחיד שיכול להוכיח את זה — ה-`bind`
על endpoint 2 נכתב אליו וחי בטבלת ה-Zigbee שלו. **אל תבטל אותו.**
קרא את ה-`short_addr` מהרישום, אל תקבע אותו — הוא התחלף ל-`0x3c20`
בצימוד מחדש.

---

## 26ב — `_request_reporting` לכל endpoint

**בהיקף:** `zigbee_gateway.py` בלבד. **רק אחרי ש-26א ירוקה.**

### הממצא

`_request_reporting` שולח `enable_reporting` עם
`entry.get("endpoint", 1)` — endpoint אחד למכשיר. מכשיר רב-גאנגי
מקבל `bind` + `configure_reporting` לגאנג 1 בלבד, לנצח. לחיצה
ידנית על גאנג 2 לא מדווחת, וכלל ההתערבות הידנית לא חל עליו.

**זה לא "הפער הידוע במולטי-גאנג" שבנספח ב׳.** שם מתואר שה-`endpoint`
העליון הוא מה שמסלול הפקודה משתמש בו. שה-`bind` בכלל לא נשלח —
לא מתואר שם.

### מה לעשות

בקש reporting לכל endpoint ברשומה שיש עליו cluster 6.

שים לב ש-`_reporting_inflight`, `_reporting_retry` והקרדיטים
ממופתחים היום לפי `ieee`. אם שני endpoints של אותו מכשיר יבקשו
במקביל, הם ידרסו זה את זה. הכרע איך — ותעד את ההכרעה.

### הוכחה

* **M1:** החזר ל-endpoint אחד. מכשיר עם cluster 6 על שני endpoints
  חייב לקבל שתי בקשות, לא אחת.
* **חומרה:** על ה-DUT, אחרי איפוס ה-bind — שני הגאנגים מדווחים.

---

## 27 — מסך "הוסף מכשיר": רשימה עם זיהוי בפעימה

**בהיקף:** `products/panel/device/` — `main.py`, `room_page.py`,
ומסך חדש. **תלוי ב-26א** (כפתור הזיהוי קורא `read_state` עם endpoint).

**מחוץ להיקף:** זיהוי בנורית Zigbee. זו משימה 28.

### מה קורה היום

`room_page._add_device` פותח `permit_join(180)` ומסמן
`store.pairing`. `main._try_autopair` חוטף את המכשיר הראשון שאין לו
ישות, יוצר ישות אחת בשם `"מכשיר N"`, ועושה `break`.

המשתמש לא בוחר, לא מזהה, ומכשיר דו-גאנגי מקבל ישות אחת.

### א. `_try_autopair` נמחק

לא "מתווסף עליו" — **נמחק**. כל עוד הוא רץ הוא חוטף את המכשיר
והרשימה תמיד תהיה ריקה. זה השינוי האמיתי, לא הוספת מסך.

### ב. `_devices_refresh` מרזה יותר מדי

הוא שומר ל-store רק `{on_off, unreachable, endpoint}` ומשליך
`clusters` ו-`endpoints`. המסך החדש לא יכול לגזור את רשימת הגאנגים
בלעדיהם.

הוסף מה שצריך — ושמור על הכוונה המקורית של השורה הזאת, שכתובה
בהערה מעליה: לא לכלול שדות שמשתנים בכל poll (`state_age_ms`),
אחרת כל poll יצייר מחדש.

### ג. מבחן שלמות הגילוי — אל תדלג על זה

ההערה ב-`_on_device_endpoints` אומרת:

> "device_joined always reports **endpoint 1** because that is all it
> knows at announce time"

רשימת ה-endpoints והקלאסטרים מגיעים **אחרי** ההצטרפות, בהודעות
נפרדות. מסך שיצייר ברגע ה-join יראה מכשיר דו-גאנגי כחד-גאנגי,
המשתמש יוסיף רק את גאנג 1 — **וזה בדיוק הבאג של היום בלבוש חדש.**

מבחן השלמות: `endpoints` קיים ברשומה, **ולכל endpoint ברשימה יש
מפתח ב-`clusters`.** עד אז השורה מציגה "מזהה יכולות…" ולא מספר
גאנגים.

### ד. הרשימה

שורה אחת לכל **גאנג**, לא לכל מכשיר:

> endpoint ברשומה שיש עליו cluster 6 (OnOff), שאין לו כבר ישות.

אומת מול המכשירים האמיתיים:

```
70:d0:7e:…  endpoints [1, 2]     cluster 6 על שניהם  → שתי שורות
78:1c:9d:…  endpoints [1, 242]   cluster 6 על 1      → שורה אחת
                                 (242 = Green Power)
```

תלת-גאנג יגיע כ-`[1,2,3]` ויקבל שלוש שורות בלי שורת קוד חדשה.

**"אין לו כבר ישות"** = אין ב-`endpoints.list` רשומה עם אותו
`ieee_address` **ואותו** `zigbee_endpoint`. לא לפי `ieee` בלבד —
זו בדיוק ההנחה שגרמה לגאנג 2 לא להופיע לעולם.

### ה. כפתור "זהה" — בפעימה

```
קרא את מצב ה-endpoint הזה  →  הפוך  →  המתן  →  החזר
```

הקריאה חייבת להיות פר-endpoint (26א). **בלעדיה** הקריאה מחזירה את
מצבו של גאנג 1, ההיפוך נשלח לגאנג 2, וההחזרה מגדירה את גאנג 2
להיפוך של גאנג 1 — המנורה נשארת במצב אקראי.

**חובה להחזיר.** אתה מדליק עומס אמיתי בבית של מישהו. אם ההחזרה
נכשלה — הודע למשתמש, אל תשתוק.

### ו. הוספה

יוצרת ישות `(ieee, endpoint)` עם ה-`zone_id` מ-`store.pairing`.

השם: **אל תשתמש ב-`"מכשיר {}".format(len(endpoints) + 1)`** — הוא
מייצר "מכשיר 2" למכשיר שאינו השני. תן למשתמש לתת שם, או גזור
מהחדר והגאנג.

### ז. זריקה מהרשימה

מחק את הרשומה מהרישום **מקומית בלבד**. אל תשלח `remove_device` —
זו משימה 21, והיא מאשרת `ok` ללא תנאי. אל תשעין זרימת משתמש על
הפעולה היחידה בקושחה שאנחנו יודעים שאין לה סולם ACK.

### הוכחה — שלוש מוטציות

* **M1:** הסר את סינון cluster 6 → endpoint 242 מופיע כגאנג.
* **M2:** החלף "אין ישות ל-(ieee, endpoint)" ב-"אין ישות ל-ieee"
  → גאנג 2 נעלם מהרשימה. **זו המוטציה המרכזית.**
* **M3:** החזר את `_try_autopair` → הרשימה ריקה כשהחלון פתוח.

ובנוסף: הסר את מבחן שלמות הגילוי (ג) → מכשיר שהרגע הצטרף מוצג
כחד-גאנגי.

### חומרה

מול ה-DUT הדו-גאנגי: שתי השורות מופיעות, והזיהוי מזיז את המנורה
הנכונה ומחזיר אותה.

---

## 32 — `forget_device` יקשיב לפסק הדין, ויסדר את הסדר

**חומרה: P2. אחרי 21, לא ממוזגת לתוכה.** 21 היא קושחה, 32 היא אדפטר — שני
קבצים, שני קומיטים.

**בהיקף:** `src/smart_kosher/adapters/zigbee_gateway.py` (`forget_device`).

### הממצא

```python
def forget_device(self, ieee):
    self._registry.pop(ieee, None)          ← שוכח קודם
    self._save_registry()
    self._write_cmd("remove_device", …)     ← fire-and-forget
    return {"removed": True}                ← ללא תנאי
```

**שני פגמים, ושניהם שורדים את 21:**

1. **אף אחד לא מקשיב.** `_write_cmd` אינו יוצר רשומה ב-`_pending`, ולכן ה-ack
   חוזר עם `request_id` שאיש לא ממתין לו **ונזרק בשקט**. גם אחרי ש-21 תלמד
   את הקושחה להחזיר `delivered`/`failed` אמיתי, הפסק דין לא מגיע לאיש. אותה
   צורה בדיוק שנמצאה ב-`_request_reporting`.
2. **הסדר הפוך.** הרשומה נמחקת **לפני** השליחה, אז `leave` שנכשל משאיר מכשיר
   ששכחנו — והוא עדיין על הרשת.

### מה לעשות

`await self._command("remove_device", …)`, ורק בהצלחה למחוק. **שלח, המתן, ואז
שכח.** שים לב ש-`forget_device` סינכרונית היום ויש לה קוראים
(`_release_radio_device` ב-`api.py`, ו-`discard_device` **אינה** נוגעת בזה —
היא מקומית בכוונה).

### הוכחה

* **M1:** החזר את הסדר → `leave` שנכשל משאיר רישום מחוק ומכשיר על הרשת.
* **M2:** החזר ל-`_write_cmd` → הפסק דין נזרק ו-`removed: True` חוזר תמיד.

---

## 36 — "מחפש מכשיר" לא נגמר לעולם  ⛔ רגרסיה מ-27

**חומרה: P1. ראשונה.** **בהיקף:** `room_page.py`, `main.py`.

`store.pairing` מנוקה **רק** ב-`room_page.py:71`, בלחיצה חוזרת על הכפתור.
לפני 27, `main._try_autopair` עשה `store.pairing.set(None)` אחרי אימוץ. 27
מחקה אותו **בצדק** — זו הייתה המשימה — ואיש לא החליף את הניקוי.

וגרוע יותר: `permit_join(180)` פג ברכזת אחרי שלוש דקות והמסך ממשיך לומר
"מחפש מכשיר (ביטול)". **התווית משקרת על מצב הרשת.**

**מה לעשות.** החלון הוא 180 שניות — שהמצב יפוג איתו, והתווית תספר את האמת
("מחפש… 2:14") במקום בוליאני שנתקע.

**לפני שממציאים טיימר:** יש אירוע `permit_join_status` מהקושחה. בדוק אם הוא
כבר נותן את הסוף. **אמת מהחוט עדיפה על שעון מקומי.**

*מוטציה:* הסר את הפקיעה → המצב נתקע שוב.

---

## 37 — אין כפתור "חבר"  ⛔ פער ב-27

**חומרה: P1.** **בהיקף:** `add_device_page.py`.

`:195` — ההוספה תלויה על הכרטיס עצמו:

```python
card.add_event_cb(lambda e, r=row: _add(r), lv.EVENT.CLICKED, None)
```

ולידו **שני כפתורים גלויים**, "זהה" ו"הסר מהרשימה" (`:199-204`). הפעולה
הראשית היא היחידה בלי כפתור, **והמשתמש לא מצא אותה.**

**מה לעשות.** כפתור "חבר" מפורש בשורה, בולט משני האחרים.

---

## 38 — אייקונים מוצגים כמלבן  · **פרוב לפני תיקון**

**בהיקף:** `theme.py`, ואז מה שהפרוב יקבע.

הם **אינם** `LV_SYMBOL` — הם תווים טיפוגרפיים בפונטי Assistant:

| תו | קודפוינט | מופעים |
|---|---|---|
| `›` | U+203A | 7 — חץ החזרה בכל תת-מסך (`shell.py:59`) |
| `‹` | U+2039 | 2 (`widgets.py:63, 100`) |
| `—` | U+2014 | **48** — מצב "לא ידוע" |
| `·` | U+00B7 | 11 |
| `…` | U+2026 | 4 |
| `±` | U+00B1 | 1 |

ובנוסף שני `LV_SYMBOL` אמיתיים מ-`FONTS.symbol`: BACKSPACE ×4, HOME ×1.

`theme.py:41` קורא לקובצי ה-`.bin` "Hebrew-ranged", וכל התווים למעלה מחוץ
לטווח עברית+ASCII. **זו השערה שלא נמדדה.** והלוגו אינו ראיה — `ui_home.py:53`
אומר "solid primary + house glyph", כלומר ריבוע צבוע; הגליף שעליו כנראה מלבן
גם הוא, על רקע מלא.

### שלב א׳ — פרוב, לפני כל תיקון

**אל תנסה לפרסר את קובצי ה-`.bin`.** ניסיון כזה החזיר 6,840 גליפים בקובץ של
5KB — כלומר לא מדד כלום. שאל את הפונט עצמו, על הלוח:

```python
dsc = lv.font_glyph_dsc_t()
theme.FONTS.body.get_glyph_dsc(dsc, cp, 0)
```

על `0x203A 0x2039 0x2014 0x00B7 0x2026 0x00B1 0xF55A 0xF015`, מול `body`
ומול `symbol`. **דווח את הרשימה לפני תיקון.**

### שלב ב׳ — ההכרעה, ומה שמייקר אותה

**לקובצי הפונט אין מתכון בנייה בריפו.** חמישה `.bin` ב-
`products/panel/device/fonts/`, בלי `lv_font_conv`, בלי מפרט טווח, בלי TTF
מקור. **אותה מחלקה כמו 0.11.1: נכס שנשלח ואי אפשר לייצר מחדש.**

1. **להחליף תווים בכאלה שבטווח** — "חזרה" בעברית במקום `›`, מקף ASCII במקום
   `—`. זול, בלי תלות בנכס.
2. **לבנות מחדש עם טווח מורחב** — ואז המתכון נכנס לריפו, אחרת יצרנו את אותה
   בעיה לדור הבא.

**הנטייה: 1 לתיקון, 2 כמשימה נפרדת** — אין סיבה שתיקון תצוגה יחכה לשחזור
נכס. הפרוב מכריע.

### שלב ג׳ — גילוי, לא אופציונלי

`load_fonts` בודק ש-`_load_binfont` לא החזיר `None`, ולא שהפונט **נושא את
התווים שהממשק מצייר**. וב-`theme.py:88` אין אפילו את זה:

```python
FONTS.symbol = getattr(lv, "font_montserrat_28", None) or lv.font_montserrat_16
```

השאלה היא "האם אובייקט הפונט קיים", לא "האם יש בו את הגליף". **הוסף בדיקה על
קומץ קודפוינטים והדפס אם חסר** — בלי זה הבנייה הבאה תשבור את זה שוב בשקט.

**השלכה על 33:** אם U+2014 חסר, ה-`—` שמשמעותו "לא ידוע" הוא מלבן ב-48
מקומות. כשמתקנים — לוודא שהמצב נראה כמו "לא ידוע" ולא כמו תקלה.

---

## 39 — אין מספרים במקלדת

**בהיקף:** `text_input.py`. **שורה אחת.**

`keyboard.py:23` כבר מכיל `("1","2","3")` — הפריסה הנומרית קיימת.
`text_input.py:102` מעביר `LAYOUT_HEBREW` בלבד.

**"מכשיר 2" ו"סלון 1" אינם ניתנים להקלדה היום** — וזה בדיוק השם שהצימוד
מייצר.

---

## 40 — אין סמן בשדה הטקסט

**בהיקף:** `text_input.py`.

`:90` — `_field` הוא `w_label`, לא `lv.textarea`. **לתווית אין סמן, מבנית.**

**בדוק לפני שעוברים ל-`lv.textarea`:** הוא מביא גלילה משלו, ו-CLAUDE.md אוסר
גלילה על הפאנל. אם מתנגש — קו נטוי מהבהב בסוף הטקסט מספיק. בלי אנימציה
מלאת-מסך.

---

## 41 — גובה שורה קשיח

**בהיקף:** `room_page.py`.

`set_height(64)`, `set_size(120, 64)`. המבוקש: רבע מהגובה הזמין, מותאם.

**זהירות:** ל-`hwtest_ui` יש "אף רשימה לא גולשת מהזכוכית" ו"עמוד מחזיק מספיק
שורות". גובה יחסי משנה כמה נכנס. **הרץ ותראה מה נשבר — ואם משהו נשבר, זו
הכרעה ולא תקלה: תגיד מה בחרת ולמה.**

---

## 42 — פסים מהמסך הקודם נשארים במעבר  · **פרוב לפני תיקון**

**המבני והחמור מכולם.**

כל המסכים נטענים דרך `lv.screen_load` על אובייקט **מטמון** שנבנה פעם ומנוקה
ב-`clean()` (`add_device_page:218`, `room_page:280`, `device_page:184`,
`pages:62`). החשד: LVGL מצייר מחדש רק אזורים שסומנו כלא-תקפים, ומסך ממוחזר
לא מסמן הכול — ועם RGB DMA זה משאיר שורות ישנות בפריים.

**הפרוב הראשון, ולא תיקון:** אחרי `screen_load`, קרא
`lv.screen_active().invalidate()`.

* **נעלמים** → פער סימון, תיקון קטן ומקומי.
* **נשארים** → שכבת DMA/פריימבאפר — למדוד לפני שנוגעים.

**אל תוסיף אנימציית מעבר כדי לכסות** — CLAUDE.md אוסר אנימציה מלאת-מסך, וזה
מסתיר סימפטום. **רק העין תופסת את זה, אז כל תיקון דורש אישור מהמסך.**

### נמדד 2026-08-25 — הפרוב שלמעלה כנראה מיותר, והתשובה כבר בזמנים

לא הרצתי את `invalidate()`, כי מדידה זולה יותר מכריעה לפניו.

| מה | ערך |
|---|---|
| מחזור פריים של הפאנל | **22.8 ms** (PCLK 18MHz; H 800+8+8+4=820, V 480+8+8+4=500 ⇒ 410,000 px ⇒ 43.9Hz) |
| ציור מחדש של מסך מלא | **114.8 ms** חציון (6 דגימות: 110.2 · 114.2 · 114.8 · 114.8 · 114.8 · 118.9) |
| היחס | **~5 פריימים לכל מעבר** |

הטווח שנכתב לפני ההרצה היה 30–200 ms; התוצאה בתוכו.

**המסקנה:** מעבר מסך *אינו יכול* להיות אטומי. הפאנל סורק את ה-framebuffer
כחמש פעמים בזמן ש-LVGL עדיין כותב לתוכו, ולכן חלק מהמסך מציג תוכן ישן וחלק
חדש — וזה בדיוק "פסים".

ומכאן ש-`invalidate()` לא רק שלא יעזור, הוא **יכול להזיק**: הוא מגדיל את
השטח שיש לצייר, כלומר מאריך את החלון שבו שתי התמונות חיות יחד. בעץ ההחלטות
של המשימה זה הענף השני — "שכבת DMA/פריימבאפר, למדוד לפני שנוגעים" — וזו
המדידה.

**מה שהמדידה עדיין לא אומרת:** האם הדרייבר מרכיב לתוך ה-framebuffer שנסרק
*ברגע זה*, או מחליף בין שניים. `display.py` מתאר "שני framebuffers מלאים
מוסתרים ב-C" עם העתקה מ-core 2. אם יש החלפה אמיתית ב-vsync לא היו פסים —
העין אומרת שיש. לפני נגיעה ב-framebuffer צריך לאמת את זה ישירות; CLAUDE.md
מזהיר על בדיוק השכבה הזאת (LoadProhibited boot-loop).

**לא תיקנתי.** בחירת אסטרטגיית framebuffer היא החלטת מוצר עם סיכון אתחול,
והמשימה עצמה דורשת אישור מהמסך.

---

## מטלה קטנה — `deploy.ps1` ימצא את הלוח לבד

`-Port` חובה, בזמן ששלושת רצי ה-hwtest קוראים `find_panel()`. הדוקסטרינג של
`run_hwtest.py` מציין את הקפיצה COM7↔COM8 כאחת משלוש המטלות שהוא פותר.
`deploy.ps1` הוא היחיד בתיקייה שלא קיבל את זה, **והוא הפיל פריסה ב-2026-08-24.**

`find_panel()` כברירת מחדל, `-Port` נשאר כעקיפה. שורה אחת.

---

## 28 — זיהוי בנורית (שיפור נוחות, **לא חוסם**)

> **עודכן:** `zigbee.identify` נבנה במשימה 30 ומזהה **בהפעלת הממסר עצמו** —
> קריאה, היפוך, החזקה, החזרה ב-`finally`, עם נעילה פר-ieee. אומת על ה-DUT
> בשני הגאנגים עם מנורה אמיתית. **זרימת ההוספה שלמה.** 28 מחליפה את ההיפוך
> בהבהוב נורית, שהוא נעים יותר ולא מפעיל עומס — אבל שום דבר לא ממתין לה.

אין `identify` ב-ops של הקושחה. שמונה בלבד: `ping`, `permit_join`,
`on_off`, `read_attr`, `read_report_cfg`, `enable_reporting`,
`remove_device`, `bind`.

המכשיר **כן** תומך — cluster 3 קיים על שני ה-endpoints של ה-DUT.
רק אנחנו לא יודעים לשלוח.

דורש op חדש ב-`zb.c` ובנייה ב-ESP-IDF. **לצד משימה 21.**

---

## חסמים עומדים, לא משימות

| מה | פותח |
|---|---|
| בנייה אחת ב-ESP-IDF | משימה 21, מאמתת 19+20, ומאפשרת 28 |
| `.gitattributes` עם `*.sh text eol=lf` | `run.sh` לא רץ ב-WSL (CRLF). הפקודה המתועדת מעולם לא רצה — ה-1,247 הגיע מקריאת gcc ישירה |

---

## 44 — חליפת ה-hwtest מחזירה 0 גם כשהיא אדומה

**נמצא תוך כדי 38.** `mpremote 1.28.0` **אינו** מעביר את קוד היציאה של המכשיר
למארח. נמדד ישירות:

```
mpremote exec "raise SystemExit(1)"  ->  EXIT=0
mpremote exec "raise SystemExit(0)"  ->  EXIT=0
```

ולכן `run_hwtest_ui.py` — שכל הציון שלו הוא
`raise SystemExit(0 if hwtest_ui.run() else 1)` — יוצא **0 גם כשהחליפה נכשלה**.
אומת: הרצת המוטציה של 38 הדפיסה `79 of 80 passed` + `FAILED:` והחזירה `EXIT=0`.
אותו דבר ב-`run_hwtest.py`.

`run_hwtest_gangs.py` **לא** נפגע: הוא רץ עם `capture=True` וגוזר את שלושת
קודי היציאה שלו מהפלט במארח. שלושת הקודים שהוכחו שם תקפים.

**המשמעות:** כל שער שנשען על קוד היציאה של חליפת הלוח ירוק תמיד. זו בדיוק
המשפחה של "ירוק כוזב במנגנון האימות" — אבל בשכבה שמעל הבדיקות, ולכן היא
מכסה את *כולן* בבת אחת.

**התיקון:** לגזור את התוצאה מהפלט, כמו ב-gangs (`capture=True` + חיפוש
`FAILED:`/`N of M`), לא מקוד היציאה. **חייב טסט שמוכיח שהוא יכול להיכשל.**

---

## 45 — deploy לא מוחק, ולכן הלוח אוגר קבצים מתים

**נמצא תוך כדי 38.** `house_page.py` נמחק מ-git ב-2026-08-02 (`10f99fe`,
"remove superseded house_page") ועדיין יושב על הפלאש — **באותם 5308 בתים**
בדיוק כמו הגרסה שלפני המחיקה. אף קובץ בריפו אינו מזכיר `house_page`.

`deploy.ps1` מעתיק קבצים ואינו גוזם. קובץ שנמחק מהמקור נשאר על המכשיר לנצח.

**למה זה לא רק לכלוך:** הסריקה של 38 נפלה עליו — היא דיווחה על תו שאינו
מצויר בשום מקום, כי הקוד שמכיל אותו כבר לא רץ. כל בדיקה עתידית שסורקת את
`/` תמדוד את מה שהפלאש **צבר**, לא את מה שהמוצר **הוא**. 38 עקף את זה בהליכה
על ה-imports; זה עוקף תסמין, לא סיבה.

**המשמעות הרחבה:** מודול מת שנשאר על הלוח עדיין ניתן ל-import. קוד ישן
שנשאר נגיש הוא הדרך שבה "תיקנו את זה" הופך ל"תיקנו את זה בקובץ אחר".

---

## 46 — `mpremote cp` יכול לכתוב קובץ באורך 0, ולדווח שהצליח

**נמצא תוך כדי 40**, בדרך הקשה: אחרי פרוב שבו `restore_main` נכשל
(`unexpected read during raw paste`), ניסיון שחזור שני **החזיר 0** ובכל זאת
הותיר על הלוח `main.py` בגודל **0 בתים**. הלוח נשאר בלי מוח.

זה בדיוק הדפוס של `clean_board שיקר` בזיכרון: פעולה על הלוח שמדווחת הצלחה
בזמן שהמכשיר במצב אחר. וזה מסוכן יותר מכישלון גלוי, כי `restore_main`
מסתמך על קוד היציאה של `cp` ומדפיס "screen is coming back".

**ההגנה הזולה:** אחרי כל `cp` של `main.py` — לקרוא את הגודל מהלוח ולהשוות
למקור. `restore_main` כבר מדפיס אזהרה כשהעתקה נכשלת; היא צריכה לאמת גם
כשההעתקה "הצליחה". רלוונטי גם ל-`deploy.ps1`.

**נצפה פעם אחת.** לא ידוע אם הגורם הוא מרוץ מול reset או משהו אחר.
