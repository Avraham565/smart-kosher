# מה פתוח

עודכן 2026-08-30. היסטוריה: `docs/lab-notebook-2026-08.md`.

## הסדר

| # | מה | דורש |
|---|---|---|
| 63 | `unreachable` נדלק גם על כישלון מסירה | כלום |
| 64 | פרוב השחזור | ממסר |
| 65 | שלבי עלייה של הפאנל | כלום (חור אחד מ-64) |
| — | צריבת 2.0.4 ומדידה | ממסר |
| — | תגובה ב-issue #898 | כלום |
| 56 | מחיצת האפליקציה מלאה ב-99.9% | בנייה |
| 58 | למה ציור כפול ניקה את הפסים | פתוח, לא באג |

---

## 61 — אחרי אתחול הרכזת אין גישה למכשירים

**חומרה: P2.** נמדד על החוט 2026-08-29.

### מה נמדד

מחזור מתח לפאנל מפיל גם את ה-H2. הרכזת משחזרת את הרשת מ-NVS
— אותו PAN, ערוץ וכתובת קצרה — ומודיעה `restored_from_nvs`.
מאותו רגע כל `on_off` חוזר `failed` עם `aps_status: 1`,
ולפעמים `send_failed`.

**החלון נסגר מעצמו כשהמכשיר מצטרף מחדש. שתי דגימות: 125.0
ו-45.6 שניות.** הטיימר שייך למכשיר, לא לנו.

**המשתנה הוא אתחול הרכזת.** בהקלטה שבה ה-uptime שלה נשאר
רציף (417→439) הכל עבד.

**מה שנשלל:** לא כתובת קצרה מעופשת — הושוותה מול גיבוי,
זהה לפני ואחרי. לא מכשיר ישנוני — הוא עונה תוך 200–400ms
ורצוף לחלוטין ברגע שהוא מחובר.

### המכשיר

`b0:e8:e8:ff:fe:66:82:bd` — ממסר **בלי אפס, שאינו מחזק את
הרשת**, כלומר **מכשיר קצה** שהרדיו שלו דלוק תמיד.

### ההשערה — לא מוכחת

הרכזת מאבדת את רשומת הילד בטבלת השכנים. בלעדיה היא לא יודעת
שהמכשיר ילד ישיר, ולכן **מנסה לנתב אליו — ומכשיר קצה לעולם
לא עונה לבקשת ניתוב.** ההצטרפות מחדש מחזירה את הרשומה, ומשם
שידור ישיר: מיידי ורצוף.

מתיישב עם `#898` (רשומת ילד ZED נעלמת אחרי אתחול, H2, 2.0.3)
ועם `#870` (הרכזת מנתבת פריים שהיה אמור ללכת ישירות).

### מה שלא ידוע

- האם טבלת השכנים באמת ריקה אחרי השחזור. **64 עונה.**
- האם שידור רחב מגיע למכשיר בתוך החלון. **64 עונה.**
- **האם התרחיש האמיתי סובל מזה בכלל.** נמדד מחזור מתח לפאנל
  בלבד, כשהממסר נשאר דלוק. בהפסקת חשמל אמיתית גם הוא נופל
  ומודיע על עצמו תוך שניות. **לא נבדק.**
- **האם catch-up באמת מנסה שוב תזמון שנפל בחלון.** נגזר
  מהתכנון, לא נמדד. **זו הטענה היחידה שמחזיקה את הדירוג
  על P2.**
- מה `aps_status: 1` באמת. **אינו קוד APS** — אלה `0x00`
  ואז `0xa4`–`0xb0`. השדה נושא ערך מספירה אחרת.

### ההשלכה, ומה מתקן אותה עכשיו

בחלון, שליטה ידנית נכשלת **בשקט**: הקישור ירוק, ping עונה,
והמכשיר "לא מגיב" בלי שהמסך אומר מילה.

הדגל `unreachable` בנוי מקצה לקצה — `zigbee_gateway.py:417`
→ `dev_common.py:92` → `widgets.py:19` → הדסקטופ. **מה שחסר
הוא התנאי:** `:1151` מדליק אותו רק על `timeout`, וכאן חוזר
`failed`. **זו משימה 63, והיא CPython טהור.**

### כיווני מבוי סתום — נבדקו ונשללו

אל תפתח אותם שוב. הפירוט ביומן.

- **פורט ל-esp-zigbee-lib 1.x** — היעד נטוש (1.6.8 מנובמבר
  2025, ו-esp-zboss-lib מפברואר 2025), **והתיקון שאנחנו
  צריכים נוסף ב-2.0.4 — כלומר קדימה, לא אחורה.**
- **מונה פריימים / מערכי NVRAM של ZBOSS** — 2.x אינה ZBOSS.
- **src-match והחזקת הודעות** — המכשיר אינו ישנוני.
- **אי-התאמת ESP-IDF** — 2.0.3 ו-1.6.8 מבקשות שתיהן `>=5.0`.
  5.3.0 תקינה.

### מה שכן מוביל

**2.0.4** (יצאה 14/08; אנחנו על 2.0.3, ו-#898 מוגש נגד 2.0.3)
מתקנת `'PanCoordinator' bit would lost after the coordinator
reboots` ומוסיפה indirect retransmission עם source address
matching. **נבנתה מול `zb.c` — נקי, אפס שינויי מקור, מול
בניית ביקורת.** לא נצרבה.

**התאמה של תיאורים, לא הוכחה.**
